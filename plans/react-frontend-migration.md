# Complete the React frontend migration

Status: **implementation substantially complete; several positive flows, full acceptance, platform acceptance and canonical cutover remain open.** Svelte is still canonical.

Reassessed **2026-09-11** at `2d8050e3a747cd3605d320440cb5a9586ecbe9e9` on `main`. This replaces the previous execution queue in place. It is the single executable migration plan; prior detailed completion history remains in Git and the cited evidence directories. This assessment changed planning only. The unrelated `.gitignore` edit is not part of the work.


## Further Ops acceptance — owner, diagnostics and public embed, 2026-09-11

The owner chose to continue other Ops cases while R03 remains undecided. The next observed failure was the directory copy confirmation: localization renders **Copied!**, but the test required **Copied**. The expectation now matches the exact localized confirmation; the exact clipboard Entity ID, directory filtering/navigation, solvency values and shared-socket cleanup assertions are preserved.

Mobile public-embed autoplay then exposed a UI interaction race: every frame read set `network.loading`, disabling the slider and interrupting keyboard focus before Home could select the first frame. Pause and the slider now remain enabled during autoplay reads. Manual selection still uses the existing playback cancellation and stale-selection rejection. The browser regression now requires slider focus to survive an actual frame advance before Home pauses at **h1 / 1 of 126**. No protected Runtime implementation changed.

Fresh evidence is in `output/react-finish-20260911/ops-next/`. Completed coverage:

| Flow | Passed cases across three viewports | Logs |
| --- | --- | --- |
| Local-only remote restrictions / Entity Audit; local inspectors close and reopen | 6/6 | `xln-ops-next-local-{laptop,rest}.log` |
| Unavailable Runtime state and persisted-chain diagnostics | 6/6 | `xln-ops-next-panels-laptop.log` (two passing cases before the copy failure), `xln-ops-next-diagnostics-rest.log` |
| Directory copy/navigation, solvency and connection disposal | 3/3 | `xln-ops-next-directory-final.log` |
| Wrong-owner rejection, secret handling, lock and expiry revocation | 3/3 | `xln-ops-next-owner-{laptop,rest}.log` |
| Local restore failure leaves keys revocable; successful recovery/unlock/relock | 6/6 | `xln-ops-next-local-owner-{laptop,rest}.log` |
| Public embed full-layout persistence/reset, malformed trail and corrupt saved layout | 9/9 | `xln-ops-next-embed-{laptop,rest}.log` |
| Autoplay focus retention and explicit keyboard pause (final regression) | 2/3; mobile and laptop pass, wide incomplete | `xln-ops-next-autoplay-focus-{mobile,laptop}.log`, 22.76 / 23.82 s |

Completed final-case evidence totals **35**. Pre-fix laptop/wide autoplay passes are retained but do not substitute for final regression coverage. Wide final autoplay reached the 30-second process limit both during a concurrent build and in a standalone retry; exact detached fixture groups were stopped. The first shared-Runtime Graph3D/history attempt reached its diagnostic 20-second test limit at Fit network, with that control present in the failure snapshot; no Graph3D behavior defect is established. Both flows remain incomplete. Large viewport batches must be split within the existing 30-second process budget; interrupted batches are retained as incomplete evidence.

Ops local checks pass: **874 files / zero unsafe-type findings**, tooling and Ops typechecks, **19.27 s**. The standalone Ops production build passes in **14.00 s**. The complete frontend unit suite passes **1,510/1,510**, **11,617 assertions**, **245 files**, **13.35 s**. Its first sandboxed run failed six localhost-server tests; the unchanged rerun with localhost permission passes. The interrupted concurrent build and its cleanup error remain in the logs; the subsequent process inspection found no remaining build process.

Final `bun run check` reaches the **30-second process limit / exit 124** during `check:src` and `check:frontend`, after passing artifact-drift and frontend-file-size checks. It does not provide complete integration evidence; the prior missing-Cargo failure remains recorded separately below. Final diff whitespace checks pass and the stand is free. Mobile/laptop autoplay screenshots were inspected. No new commit, push or deployment was made. R03 remains unchanged and pending. Other registered Ops cases still require acceptance; this table does not close the entire matrix.

## Ops matrix continuation — localized test selectors, 2026-09-11

Starting from `86765de2d`, the next failures were three test selectors that no longer matched the localized UI. Database refresh now targets **Refresh** inside the database pane; performance-policy confirmation is scoped to `runtime-performance-budgets` so the XR scale output cannot collide with its status; Runtime I/O matches **Accounts** case-insensitively and asserts the Entity summary is visible before opening it. Existing data, ownership, history, persistence and no-browser-error assertions remain. Only frontend-owned tests and this plan changed; no UI or protected Runtime implementation changed.

Clean completed browser runs: **21/21** across 390×844, 1366×900 and 1920×1080. Related Ops/inspector/policy units pass **108/108**, **1,874 assertions**, **19 files**, **0.39 seconds**.

| Flow | Viewport coverage | Evidence log / process time |
| --- | --- | --- |
| Database paging/search/store switching/refresh and unchanged row count | 3/3 | `xln-ops-database-final.log`, 12.11 s |
| Exact storage/performance policy save and reopen | 3/3 | `xln-ops-policy-final.log`, 11.40 s |
| Locale persistence and effect/renderer/XR settings | 6/6 | `xln-ops-locale-effects.log`, 22.25 s |
| Runtime attach/select plus expiry/retry/bulk validation | 6/6 | `xln-ops-runtime-manager-laptop.log`, 13.00 s; `xln-ops-runtime-manager-rest.log`, 17.80 s |
| Scenario Console, Runtime I/O and historical J-Machine inspection | 3/3 | `xln-ops-io-{mobile,laptop,wide}.log`, 22.67 / 16.20 / 23.29 s |

Evidence is under `output/react-finish-20260911/ops-matrix/`. Database, policy, locale and Runtime I/O screenshots were inspected at all three sizes. Initial broad batches and the stale Runtime I/O action reached the 30-second process limit; exact detached fixture groups were identified and stopped before reruns. Bounded failures and traces are retained. Interrupted runs are not counted as full-matrix acceptance.

Final `bun run check`: **exit 127 / 22.92 seconds**, `cargo: command not found`. Retained Svelte check/build pass; repository integration remains incomplete. `git diff --check` passes, the stand lock is free, and no fixture process groups remain. No new commit or deployment was made.

**R03 remains pending the owner's destination choice.** The explicit question offers the plan's recommended remote default plus local-create/recover entry, or a destination chooser. No answer has been received and no destination behavior has been implemented. The full Ops matrix remains open; after the completed rows and scenario-inspector case, continue the remaining `ops-workspace-local-panels.spec.ts` cases individually, then the registered panel/Graph/owner/public-embed cases. Keep the real guide/native BrainVault and protected dependency requirements visible.

## Ops independent-panel history — assertion timing repaired, 2026-09-11

The owner selected the remaining Ops history failure. The retained failure was an exact-height assertion expiring at **5 seconds** while the canonical `history-frame-batch` read was still replaying WAL. Two fresh unchanged-spec runs passed with **4.548 s** and **3.695 s** reads. The repaired mobile run then demonstrated a successful **6.671 s** read, beyond the old assertion budget and within the existing remote adapter's **15-second** historical-read allowance. Only that completion assertion adopts the existing allowance; the 30-second process budget remains. No Runtime, persistence, transport or UI implementation changed.

The existing browser regression now also requires an enabled height input, its exact requested value, a ready Entity projection, and the exact retained history label after panel reactivation. All original independent selection/navigation, primary-panel Live state, one shared socket and zero remaining connections on teardown assertions remain. Fresh final evidence is **3/3** at 390×844, 1366×900 and 1920×1080, with process times **16.10 / 13.49 / 12.51 seconds**, no browser errors, and inspected history screenshots. The focused exact-history unit suite passes **5/5**, **15 assertions**. Logs and screenshots: `output/react-finish-20260910/ops-history/`.

Final `bun run check` remains **exit 127 / 20.62 seconds**, `cargo: command not found`; retained Svelte check/build pass, but the repository aggregate is incomplete. `git diff --check` passes and the fixture process groups are stopped.

The earlier `RUNTIME_INPUT_INGRESS_AFTER_PERSISTENCE_PAUSE` / `watcher.fatal_exit` did not recur across five fresh fixture runs. That separate observation is not claimed fixed; no protected implementation was changed. This closes the observed independent-panel history acceptance failure, not the complete Ops matrix or remaining R03 work.

## B9 — resolved by offline attestation migration, 2026-09-11

The owner selected B9 after the I09/V04a/V06a–V08a work below. All **22** saved attestations used the retired three-field Hanko ABI; the canonical codec already requires the fourth `memberSignatures` field. No canonical decoder change was needed. The offline `tools/release-snapshot/migrate-hanko-envelopes.ts` validates the old encoding by exact round-trip, appends an empty contract-member-signature list, then verifies the current proof against the unchanged compiled Foundation trust anchor. It preflights every manifest/snapshot binding before any writes. **44 Hanko strings in 23 files** were migrated; original packed EOA signatures, board claims, envelope digests, timestamps, source commits, snapshot roots, frozen roots, metrics and all other JSON values remain identical. No private keys were read and nothing was re-signed or published.

```bash
bun tools/release-snapshot/migrate-hanko-envelopes.ts . --check
# Offline application to an unmigrated checkout:
bun tools/release-snapshot/migrate-hanko-envelopes.ts . --write
```

After application, `--check` reports **0 pending files / verified=true**. The historical 0.1.31 attestation is retained as a regression fixture. Tests prove exact recovery of the original three-field bytes, unchanged signed fields, idempotence, rejection of forged board/threshold/signer-count claims, signature corruption and noncanonical padding; the live decoder still rejects the retired envelope. The existing Hanko tamper test now changes its final byte to `01`: replacing the final byte with `00` had become a no-op because the new final array is empty.

Fresh evidence in `output/react-finish-20260910/b9/`: focused pilot/migration **8/8**; complete frontend suite **1,510/1,510**, **11,617 assertions**, **245 files**, **8.56 seconds**; `/releases` **3/3** across all viewports, with inspected screenshots and no browser errors. The broader Foundation suite plus focused tests is **20/21**: its only remaining failure is the existing root package version **0.1.32** versus `VERSION` **0.1.31**. No version metadata was changed. Final root check remains **exit 127 / 21.74 seconds**, `cargo: command not found`; it does not invalidate B9's now-green signed-data checks.

Final B9 artifact: **473 files**, `sha256-c5f7100e8f48273d4b5670243df18692c9c9316283387da30d51fbe961492b31`. All-app local check **25.95 s**, prepare **1.54 s**, four builds **8.77 s**, assembly **1.97 s**, release-byte verifier and all **22 artifact-contained signature/snapshot bindings** pass. These stages ran separately within the 30-second budget; a single-process full aggregate was not claimed. Existing immutable candidates were preserved. No production activation was performed.

## Newly identified gaps — implementation update, 2026-09-11

Implemented on `main` at `005f6e306f776384c3a1a5b22f24819a72e5f35d` plus the working diff. The unrelated `.gitignore` edit remains untouched. This closes the three tooling/ownership gaps requested by the owner; it does not close the full migration or authorize cutover.

- **I09a–I09e:** 68 direct retained imports → **0**. The closure comprised **40 modules: 34 implementation moves and six forwarding modules redirected to existing owners**. All 34 implementation bodies compare equal after import normalization. Retained Svelte, frontend units, and eight frontend-consuming CLI/native/core tests now use the same canonical owners. No Svelte component was deleted. Syntax-aware boundary tests resolve relative/alias paths, type imports, re-exports and literal lazy imports, include bridges, follow transitive frontend owners, and reject retained source, Svelte dependencies and cross-app bootstraps. Boundary/inventory checks: **19/19**. Unsafe-type scanning now includes bridges: **874 files / 0 findings**.
- **V04a:** `--all --level=frontend` runs `bun test tests/frontend` once from the repository root before prepare/build/assembly. Local and slice selection remain narrow. Failure propagation is tested with a real failing child. Required React CI evidence coexists with the retained Svelte step; the unapplied cutover patch delegates to one aggregate and removes duplicate CI builds. **V04 remains red:** aggregate **1,505 pass / 1 fail, 11,602 assertions, 244 files**, stops at `FRONTEND_STEP_FAILED:frontend-units:1`; only the existing release-signature B9 assertion fails. No filtering or weakened release assertion.
- **V06a/V08a:** both lifecycle servers require two explicit, distinct, verified releases. Install/update serving reads only those releases; corruption is confined to a disposable copy. Shared invocation verifies both inputs before and after Playwright, preserves nonzero exits, and logs both release IDs. **16/16 lifecycle/verifier units**, including both real servers after deleting every per-app build directory, plus missing/equal/corrupt/changed-input rejection. Existing real PWA **1/1** and activation/corruption/rollback **1/1** pass under the stand lock; both input releases remain unchanged.

Supported lifecycle CLI, from **frontend** (both paths are required; additional Playwright flags follow them):

```bash
bun run test:pwa:candidate <install-release-directory> <update-release-directory>
bun run test:deployment:candidate <install-release-directory> <update-release-directory>
```

Both verified runs used install `sha256-39e601c2bc99b08c0c6de945711c0e1fff1fbb908607a098e469fba29783da22` (**473 files**) and explicit historical update `sha256-3d3d194d784e98cd78377996229eb3c396df8bb07a28a5083942d29691bb6718`. “Update” describes the test role, not chronological release ordering or a production rollback target. All four production app builds passed in **9.63 seconds**; assembly passed in **1.80 seconds**. Evidence is under `output/react-finish-20260910/i09/`.

Focused I09 browser evidence: Formation, remote Hub discovery and recovery **9/9** across 390×844, 1366×900 and 1920×1080; local Ops owner unlock/command/relock **1/1** at 1366×900. Screenshots were inspected at all three viewport sizes and these passing flows assert no browser errors. Recovery's stale URL assertion was corrected to require the exact selected Entity parameter. The initial combined Wallet run hit the 30-second budget; its detached fixture group was identified and stopped before the split runs. **Historical Ops independent-panel/history failure (superseded by the update above)**: the combined run stayed at `Reading h14`; standalone retry logged `RUNTIME_INPUT_INGRESS_AFTER_PERSISTENCE_PAUSE` / `watcher.fatal_exit`, then reached the 30-second limit. No fixture listeners/processes remained afterward. This is recorded evidence, not permission to change protected Runtime persistence behavior or claim the full Ops matrix passes. Logs: `xln-wallet-*.log`, `xln-recovery-mobile.log`, `xln-ops-browser.log`, `xln-ops-session.log` in the evidence directory above.

Final `bun run check`: **exit 127 in 22.73 seconds**, `cargo: command not found`; artifact-drift/file-size checks and retained Svelte check/build pass, while aggregate cancellation leaves V09 incomplete. `git diff --check` and `git apply --check docs/frontend/react-frontend-cutover-commands.patch` pass. The eight retargeted frontend-consuming test files and tooling/lifecycle contracts pass (**55 tests across 12 files**, combining the final focused runs). Root log: `output/react-finish-20260910/i09/xln-root-check.log`. No source changes to Runtime/Entity/Account implementations, financial formulas, contracts, or frozen-core metadata.

The detailed I09/V04a/V06a–V08a instructions below document the implemented scope. Remaining product, protected protocol, native and release checkpoints continue to apply.

## Outcome and boundaries

Deliver independently checkable and buildable **Site, Docs, Wallet and Ops** React apps, one frontend package/lockfile, namespaced assets, and one same-origin immutable release with whole-release rollback. Migration completion includes canonical dev/build/check/package/CI/native consumers and retirement of Svelte-only code. Production activation is a separate release operation.

- Preserve reachable behavior, exact commands and financial helpers, owner authority, vault formats, storage origin/keys, workers, service-worker scope, redirects, CSP and native deep links. `frontend/config/surfaces.ts` remains the route/asset authority; `localhost:8080` remains the local public entry.
- Preserve `xln-workspace-layout`, 14 default / 16 registered panels, and compact selection only for `!embedMode && width <= 760`. The internal four-panel workspace uses `storage: null` and cannot overwrite the public layout.
- Work within `frontend/**`, frontend-owned tests, this plan, and minimal root/CI/native/artifact-consumer wiring. Match `docs/fints.md`. Pure decoding belongs in Runtime-client; browser effects and subscriptions belong in browser/bridge owners with explicit cleanup; React presentation belongs in app/shared UI owners.
- Do not change Runtime/Entity/Account transitions, consensus, financial formulas, contracts, custody policy, persistence schemas or `frozen-core.json`. Protected dependency work below needs its own owner-authorized scope. Never run `frozen-core:approve`.
- Continue on the current checkout, preserving unrelated changes. No worktree, external auditor, deployment, Svelte deletion or production cutover is authorized by this planning request. On implementation resumption, routine frontend work follows the temporary override; product choices and release authority retain their explicit checkpoints.

## What is already implemented

These are implemented foundations to preserve, not a new porting backlog. Evidence describes the tested revision; it does not certify later changed bytes.

| Area / previous IDs                                           | Current implementation and retained evidence                                                                                                                                                                                                                                                                                                                                                                                                                              | Remaining boundary                                                                                                                                     |
| ------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Four application roots and release tooling                    | `frontend/apps/{site,docs,wallet,ops}`, `frontend/config/{surfaces,generated-inputs,development-gateway}.ts`, `frontend/scripts/{prepare,build,assemble,check}.ts`. App checks/builds, gateway routing and immutable assembly exist.                                                                                                                                                                                                                                      | Canonical package commands and production consumers still use Svelte.                                                                                  |
| Site and Docs; V02, R04d                                      | All public route implementations, Docs catalog/search/selection/anchors/history/error handling. Docs recorded 15/15 browser cases. `output/react-finish-20260910/r04d-browser.log`.                                                                                                                                                                                                                                                                                       | B9 signed-data acceptance passes; refresh Site's market-cap case that previously saw a 504.                                                                 |
| Wallet; W01–W09, W12–W16, W18, W08d2                          | Ownership release/propose/activate; identity and recovery; local/remote Formation and Hub opening; Account rail/tools; profile/theme/recovery/consensus; shared Stack Manager; settlement proposal review; batch recovery; collateral finality; dispute finalization; debt enforcement; cross-j route/review/submission; dock consumers. `frontend/apps/wallet/src/`, `frontend/bridges/wallet/`, `output/plan-wallet-20260908/`, `output/react-finish-20260910/w08d2-*`. | Remote settlement approval/execution W10/W11; cross-j cancellation finality W17; Lending W19; lossless Activity W20. W18c inherits W17's finality gap. |
| Local settlement; W10/W11 local portions                      | Real local peer approval, designated execution and chain finality; immutable context reviews, cancellation and duplicate prevention. `frontend/tests/react-candidate/wallet/wallet-settlement.spec.ts`, `output/playwright/react-wallet/w11-*`.                                                                                                                                                                                                                           | Local success does not establish remote projection parity B3.                                                                                          |
| Ops; O01–O12, R01/R02                                         | Shared local/remote/scenario workspace and playback; reachable Architect controls, jurisdiction reads, stack deploy/register, remote BrainVault and incidents. Owner unlock cursor and historical-label regressions fixed. `output/plan-ops-20260908/`, `output/react-finish-20260910/r01-*`, `r02-*`.                                                                                                                                                                    | Remote-to-local BrainVault entry R03; complete matrix acceptance. Unreachable Architect Build mode remains excluded.                                   |
| Graph, localization, guide; G01–G03/G04a/G05, L01–L03         | Graph effects, requested WebGPU behavior, remote graph, XR controls/unsupported state, shared locale preferences and real guide streaming/cancellation. `output/playwright/react-graph-remote-report/`, `react-ops-offline-report/`, `react-guide-online-report/`.                                                                                                                                                                                                        | Supported-headset input/teardown G04b is not established by desktop evidence.                                                                          |
| Shared ownership; I01–I08                                     | Large extraction of helpers, neutral observable stores, vault/worker/recovery/session lifecycle and Runtime bridges. Four React app roots have no direct retained-tree imports. `output/framework-isolation-20260909/README.md`.                                                                                                                                                                                                                                          | I09 closed by the implementation update above; full positive-flow and release acceptance remain separate.                                              |
| Artifact/browser/platform preparation; R05, V05b/V06/V07d/V08 | Immutable artifact interaction runner; recorded 9/9 artifact interactions, PWA 1/1, isolated activation/rollback 1/1, extension 3/3; native staging/copy tests 16/16. `output/react-finish-20260910/v05b-release-*.log`, `v06-final-release.log`, `v08-final-release.log`, `v07d1-extension.log`, `v07-final-*`.                                                                                                                                                          | Full development matrix, iOS/Android/desktop/headset acceptance, final candidate binding; lifecycle-runner inputs are now pinned.                              |
| Cutover preparation; C01a                                     | Review document, unapplied command patch and 177-path candidate retirement list exist in `docs/frontend/react-frontend-cutover-*` and `react-frontend-retirement-files.txt`.                                                                                                                                                                                                                                                                                              | Patch changes only frontend package commands/dependencies and one CI step; remaining consumer changes are described, not implemented.                  |

R04a–R04e unit-contract repairs and R06 inventory wording updates are also complete. Resolved dependencies B4/B5/B7/B10/B11 remain closed: remote graph DTO, real guide upstream, Ownership action-state projection, history-floor cursor and historical labels. B6a–B6i source/integration repairs are historical completed work; B6's current host limitation remains.

## Fresh verification and evidence limits

| Check performed at the assessment SHA                                      | Result                                                                                                                                                                                                                |
| -------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Root `bun test tests/frontend`, with localhost networking permitted        | **1,500 pass / 1 fail; 11,780 assertions; 244 files; 5.75 seconds.** Only `tests/frontend/tooling/pilots/frontend-releases-pilot.test.ts:34` fails. Log: `/private/tmp/react-migration-audit-units-network.log`.      |
| Frontend `bun scripts/check.ts --all --level=local`                        | Exit 0: tooling + all four app typechecks; unsafe scan **833 files / 0 findings**. Log: `/private/tmp/react-migration-audit-local.log`. This is not an aggregate build/browser pass.                                  |
| Frontend `bunx playwright test --config playwright.react.config.ts --list` | **414 registered cases / 40 files**, across 390×844, 1366×900 and 1920×1080. Registration only; no fresh browser run.                                                                                                 |
| Frontend `bun scripts/checks/parity-audit.ts`                              | **20 routes: 18 complete / 2 partial; 12 capabilities: 7 implemented / 5 in progress; 3 declared gaps.** These are inventory classifications, not passing-flow percentages.                                           |
| Candidate verifier on the explicit release below                           | Exit 0; **473 files**, release hashes intact. Previous 9/9 artifact, 1/1 PWA and 1/1 rollback logs were inspected, not rerun.                                                                                         |
| `git apply --check docs/frontend/react-frontend-cutover-commands.patch`    | Exit 0. Applicability does not prove that the partial patch is a working cutover.                                                                                                                                     |
| Root `bun run check`, process-group bounded                                | **Exit 127 in 20.2 seconds: `cargo: command not found`.** Artifact-drift/file-size gates pass. Retained Svelte/build children report passes, but aggregate fails. Log: `/private/tmp/react-migration-audit-root.log`. |

The first sandboxed unit run had four additional local socket failures; the permitted-network rerun eliminates all four without edits. They are environment failures, not a networking refactor backlog. No production service, heavy E2E, native installation, external CI run or device was exercised during this assessment.

Verified historical candidate:

```text
frontend/.artifacts/releases/sha256-3d3d194d784e98cd78377996229eb3c396df8bb07a28a5083942d29691bb6718
```

The cutover review still names the earlier 470-file `sha256-c6a559bf37c43a6e26bc3b0b99e9af40180faec821b674d70912222592abf517` candidate. Its rollback ID is another local candidate, **not an inspected production rollback target**. Refresh binding after the final changes; never infer a release by newest directory or checkout SHA alone.

## Vetted remaining findings

Effort: S = hours, M = about a day, L = multiple days including verification; excludes waiting for owners/devices. All findings below are high-confidence code observations. Protected protocol remedies remain decisions for their owners.

| Priority / ID  | Finding and impact                                                                                                                                                                                                                                                                                    | Evidence                                                                                                                                                                                                                         | Effort / change risk                                            |
| -------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------- |
| Closed / I09 | All 68 direct edges removed; 40-module closure has one canonical owner per implementation. | Implementation update above and resolved boundary guard. | Complete within requested scope. |
| Closed / I09 guard | Syntax-aware resolution includes bridges, aliases, type/lazy imports, re-exports and transitive frontend dependencies. | `tests/frontend/tooling/frontend-shared-boundaries.test.ts` | 19/19 boundary/inventory checks. |
| Closed / V04a | Full aggregate includes all frontend units exactly once; required CI retains Svelte evidence. | `frontend/scripts/check.ts`; `.github/workflows/build-and-test.yml` | Guard complete; B9 remains visible. |
| Closed / V06a–V08a | Lifecycle runners require explicit verified release inputs and never rebuild them. | `frontend/scripts/test-lifecycle-candidate.ts`; real PWA and deployment logs. | Units 16/16; browsers 2/2. |
| P1 / R03 (B12) | Remote-selected BrainVault entirely replaces local onboarding, so the registered canonical-backup recovery flow has no entry.                                                                                                                                                                         | `frontend/apps/ops/src/workspace/panels/ops-brainvault-panel.tsx:33`; `frontend/tests/react-candidate/ops/panels/ops-workspace-brainvault.spec.ts:7`.                                                                            | M / medium, product destination choice pending.                 |
| P1 / C01b–C02  | Canonical consumers still expect Svelte output and one root HTML entry. The command patch alone cannot launch/package the four-root release.                                                                                                                                                          | `scripts/release/build-xlnfinance-package.ts:26`; `core/api/server/static-assets.ts:60`; cutover review consumer table.                                                                                                          | L / high at activation; preparation can proceed now.            |

## Execution order and current task states

**The three newly identified gaps are implemented.** Continue with the remaining product/protected-owner decisions and non-activating consumer preparation. Full acceptance still requires B9 and the other stated prerequisites.

| Order / task                     | Current state                                              | Dependency and deliverable                                                                                                                       |
| -------------------------------- | ---------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1. I09a–I09e | Implemented | Zero resolved retained-source edges; see fresh verification above. |
| 2. V04a | Implemented; all frontend units now pass | B9 is resolved. Units, typechecks and generated inputs pass; full acceptance still includes release/platform evidence. |
| 3. V06a–V08a | Implemented; real lifecycle checks pass | Explicit install/update release CLI; both releases verified before/after. |
| 4. R03                           | Product decision required                                  | Recommended explicit local/remote destinations below; implement after that decision.                                                             |
| 5. B2, B1, B3, B8            | Protected scopes required                                  | Separate owner work, narrow reproduction first; then rerun their existing frontend positive flows. B9 is resolved by the signed-data migration above. |
| 6. C01b and V07/G04b readiness   | Preparation ready; protected parts/device runs conditional | Prepare complete consumer patch and platform execution environments while other dependencies are pending. No production activation.              |
| 7. V01–V09/G04b final acceptance | Pending final source and relevant dependencies             | Full units/dev matrix; explicit immutable release; platform/device results; root integration.                                                    |
| 8. C02                           | Owner cutover authorization + accepted evidence required   | Switch canonical consumers atomically, retire Svelte-only code and coexistence wiring, rerun final gates.                                        |
| 9. C03                           | Separate release authorization required                    | Deploy prebuilt verified bytes and prove production smoke/rollback. Outside the definition of source migration completion.                       |

```text
I09 + V04a + R03 + protected positive-flow fixes -> V04 + V05a
V06a/V08a + final source -> assemble once -> V05b + V06 + V07 + V08
platform readiness -> V07; supported headset -> G04b
C01b preparation + B13 integration authority -> reviewable complete cutover
all required acceptance + V09 + owner authorization -> C02 -> separate C03
```

Do not run a full known-red matrix repeatedly. Complete unaffected steps while decisions are pending. Existing blockers do not justify weakening assertions, inventing compatibility readers or starting a new audit campaign.

## I09 — finish shared ownership without changing behavior

**Scope:** retained frontend TypeScript helpers and their actual importers in apps/packages/bridges/tests; corresponding ownership inventories. No Runtime implementation, wire schema or financial calculation changes. Do not delete Svelte components in this step.

First reproduce the concrete boundary:

```bash
# Repository root. Initial result: 68 matching import lines, 26 importing files. Current result: zero.
rg -n 'src/lib' frontend/apps frontend/packages frontend/bridges
bun test tests/frontend/tooling/frontend-shared-boundaries.test.ts
```

The passing current test plus these imports is the problem. Start with the canonical helper already extracted: `frontend/src/lib/utils/boundary/index.ts` is only `export * from '../../../../packages/runtime-client/src/boundary';`. Update its bridge callers directly; do not create another wrapper.

| Child                                     | Exact starting owners and ordered work                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Narrow verification from repository root                                                                                                                                                                                                                                                                                                      |
| ----------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| I09a — forwarding imports and guard       | Redirect the five pure forwarding targets: `utils/boundary/index.ts`, `components/Entity/payments/runtime/payment-command.ts`, `stores/commands/{embeddedRuntimeCommandCompletion,runtimeCommandIntent,runtimeCommandJournalKeyring}.ts` under `frontend/src/lib/`. Resolve each existing re-export to its canonical owner. Extend the boundary test's import analysis to normalized relative paths, aliases, type imports, re-exports and literal dynamic imports; include bridges. During subsequent moves record exact remaining edges rather than declaring independence. Do not create a permissive wildcard allowance.                                                                                                                                    | `bun test tests/frontend/tooling/frontend-shared-boundaries.test.ts tests/frontend/runtime/session tests/frontend/recovery`. Add parser/resolver regression cases for the actual missed path spellings and the existing dynamic import. Gate the final zero-edge assertion when I09e closes; keep intermediate incompleteness visible.        |
| I09b — Runtime read/session helpers       | Trace `stores/{runtimeQueryClient,runtimeHistoryStore,runtimeViewStore}.ts`, `stores/commands/runtimeCommandBus.ts`, `utils/runtime/{liveRuntimeEnv,debugSurface,wireDebug}.ts`, `utils/control/activeTabLock.ts`, `stores/ui/{tabStore,toastStore}.ts`, and `view/local-runtime-selection.ts` under `frontend/src/lib/`. Move browser effects/adapters to existing browser/bridge ownership; pure view data remains Runtime-client/UI. Update retained and React consumers together, including dependencies discovered by following these imports. Preserve one active Runtime, query cache, command journal, singleton store and teardown path.                                                                                                               | `bun test tests/frontend/runtime/session tests/frontend/runtime/query tests/frontend/runtime/timeline tests/frontend/runtime/live-runtime-env.test.ts`. Preserve cancellation, stale-generation, command-id/replay and exact Runtime selection checks.                                                                                        |
| I09c — vault/recovery edges               | Trace `utils/onboarding/remoteRuntimeValidation.ts`, `utils/identity/entityFactory.ts`, `utils/recovery/{pushWakeRegistration,recoveryDiscoveryStatus,recoverySettings,storageSchemaRecovery}.ts`. Move UI orchestration and adapters without altering crypto, owner validation, recovery formats or durable state. Include the dynamic import from `bridges/vault/vault-store.ts`; a static-import-only move is incomplete.                                                                                                                                                                                                                                                                                                                                    | `bun test tests/frontend/onboarding/runtime/remote-runtime-import.test.ts tests/frontend/onboarding/runtime/wallet-runtime-opening.test.ts tests/frontend/onboarding/recovery tests/frontend/onboarding/brainvault tests/frontend/recovery`. Retain wrong-owner, recovery failure, abort, relock and secret-lifetime assertions.              |
| I09d — Formation and financial view edges | Trace `components/Entity/{core/entity-panel-model,assets/external-wallet-snapshot,payment-routing,payments/payment-panel-view,payments/runtime/payment-route-quote,swap/commands/same-j-swap-command,swap/swap-panel-helpers}.ts`, `components/Entity/onboarding/{onboarding-hub-join,onboarding-runtime-projection,onboarding-targets,formation/formation-commands,formation/formation-runtime-projection}.ts`, and `frontend/src/lib/native/external-wallet-authority.ts`. Existing canonical implementations remain the source of behavior; some are adapters with legitimate core imports and must not be pushed into the pure package. Fix the two type imports in shared `wallet-formation.ts`. Do not duplicate pricing/routing/formulas or board logic. | `bun test tests/frontend/onboarding tests/frontend/payments tests/frontend/markets tests/frontend/swap tests/frontend/assets tests/frontend/workspace/panels/entity-panel-model.test.ts`. Preserve exact command/context and amounts; the move must not claim to fix B1/B3/B8.                                                                |
| I09e — close the dependency graph         | Trace newly exposed transitive retained imports until apps/packages/bridges no longer depend on `frontend/src`. Update all real consumers and typed inventories; remove moved original TS implementations when no consumer remains. Keep one implementation throughout. Final guard resolves imports instead of matching a convenient substring, including direct cross-app bootstrap restrictions and absence of Svelte runtime dependencies.                                                                                                                                                                                                                                                                                                                  | `bun test tests/frontend/tooling/frontend-shared-boundaries.test.ts tests/frontend/tooling/frontend-capability-coverage.test.ts tests/frontend/tooling/frontend-platform-inventory.test.ts`; all-app local check and affected production builds. Final scan above has zero matches, and the resolved graph has no retained-source dependency. |

The 35 direct targets are a starting set, not the transitive total or permission for unrelated cleanup. Do not impose arbitrary size-driven rewrites on large lifecycle modules. Match the existing neutral observable-store pattern in `frontend/packages/runtime-client/src/observable-store.ts`. Source paths alone do not imply a Svelte runtime dependency: 30 targets have implementation statements, but most already use neutral stores.

For each family, compare implementation bodies and canonical commands before/after; change ownership/imports only. Run browser proof if observable behavior changes. At closure rerun existing owner unlock, Formation/Hub, recovery and Wallet/Ops workspace flows using the same real isolated fixtures. Stop only the affected family if preservation would require a protocol/custody choice.

## V04a — make frontend acceptance include all frontend units

**Scope:** `frontend/scripts/check.ts`, `frontend/scripts/checks/`, `tests/frontend/tooling/build/frontend-check-scope.test.ts`, frontend-owned CI wiring and the unapplied cutover review patch.

1. Preserve quick `--level=local` and one-spec `--level=slice`. For full `--all --level=frontend`, run `bun test tests/frontend` from the repository root once, before expensive preparation/build/assembly. Preserve the useful narrow per-surface contract selection; do not run the full suite twice.
2. Extend command-scope tests to require the full-suite command, correct cwd, nonzero failure propagation, and unchanged local/slice selection. The four currently selected contract files are included in the whole suite.
3. Add React checks as required CI evidence during coexistence without removing the retained Svelte gate. A separate job may stay red for B9; neither `continue-on-error` nor test filtering is acceptance. Keep generated-input and build order explicit.
4. Reconcile C01's command patch so canonical `check` delegates to this one owner and does not append a duplicate whole-suite run.

**Verify:** root `bun test tests/frontend/tooling/build/frontend-check-scope.test.ts` passes; frontend `bun scripts/check.ts --all --level=frontend --explain` includes the whole suite once. On current data the full command must surface B9; completion of V04a means the guard is correct, while **V04 acceptance remains red**. After B9, the aggregate must pass through assembly with zero failures.

## V06a–V08a — pin lifecycle runs to reviewed release bytes

**Scope:** `frontend/scripts/pwa/pwa-candidate-smoke-server.ts`, `frontend/scripts/deployment/deployment-candidate-smoke-server.ts`, their Playwright configs, package scripts and existing build/lifecycle tests.

1. Follow `frontend/scripts/test-artifact-candidate.ts`: require an explicit verified release directory, record release ID, verify before/after and reject missing or changed inputs. Add a shared invocation boundary only where both lifecycle consumers actually reuse it.
2. Replace each smoke server's baseline `assembleCandidateRelease(FRONTEND_ROOT)` with the supplied release. The acceptance command must not call prepare/build or read mutable per-app build directories. Retain a separately named build/preparation command if useful.
3. Derive the update/corruption cases from disposable copies through the existing manifest/assembly owners, or accept a second explicit verified release. Never mutate the baseline release. Preserve install, offline, update, rejected incomplete update, whole-cache rollback, push wake and deployment activation/corruption/rollback assertions.
4. Test with per-app build directories unavailable in the isolated test workspace; an explicitly supplied valid release must still work. Verify wrong release/mutated bytes fail loudly. Do not invent a second route policy.

**Verify:** root `bun test tests/frontend/tooling/build/frontend-pwa-candidate.test.ts tests/frontend/tooling/build/frontend-deployment-candidate.test.ts tests/frontend/tooling/build/frontend-candidate-release-verifier.test.ts` passes. Then run both real lifecycle specs on the explicit release under the stand lock. Their supported release-selection CLI is a deliverable of this step; record its exact syntax here when implemented. The `test:pwa:candidate`/`test:deployment:candidate` commands now take the explicit install/update directories shown above and do not rebuild.

## R03 — preserve both BrainVault destinations

**Decision pending:** recommend keeping selected-remote-node derivation as the initial remote destination, with an explicit **Create/recover local Runtime** action in the same panel. Choosing that form does not change the active Runtime; only a successful existing local-open operation hands the exact opened Runtime to the shared workspace. Cancel leaves the remote selection intact. Do not silently choose the destination on the owner's behalf during implementation.

**Scope after decision:** `frontend/apps/ops/src/workspace/panels/ops-brainvault-panel.tsx`, `ops-remote-brainvault.tsx`, existing onboarding presentation adapters and related tests.

1. Add explicit destination state and invalidate/abort its operation on panel close, destination change or selected Runtime change.
2. Preserve the existing `mounted`, exact-adapter and opened-runtime checks in `opened()`; reuse `IdentityOnboarding` and `selectWorkspaceRuntime('embedded')`. Do not add a second recovery/derivation engine.
3. Extend the existing local recovery test to choose the new explicit entry, then retain every backup/owner/shared-panel assertion. Keep the remote native derivation success/cancel test. Add meaningful cancellation, context-switch and no-secret-persistence checks using existing fixtures.
4. Verify focused onboarding/recovery/BrainVault units, Ops local checks, and `frontend/tests/react-candidate/ops/panels/ops-workspace-brainvault.spec.ts` across all three viewports. The current two tests expand to six cases; all must pass, plus new cases. Inspect screenshots/F12. Test-declared 150/180-second timeouts do not themselves grant a longer repository process budget.

## Protected dependencies — precise handoffs, no UI workaround

The following retain their previous IDs. Report each handoff with **current SHA, last green command, first red command/error, artifact path, next single reproduction command, remaining final gates**. Reproduce on an isolated fixture when its authorized owner starts; historical backend failure reports are not freshly rerun by this assessment.

| ID / blocked flow        | Evidence and first boundary                                                                                                                                                                                                                                                                                               | Required owner outcome and frontend acceptance                                                                                                                                                                                                                                                                                                                                                   |
| ------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| B9 / V01, V04 — resolved | Offline migration preserves original signatures and signed data; all 22 proofs and snapshot bindings pass the current canonical verifier. | Frontend units 1,510/1,510; `/releases` 3/3. No live compatibility decoder or trust-policy change. See B9 update above. |
| B2 / W20                 | `core/storage/queries/history.ts:456` accumulates whole-frame events, slices to limit, then advances to the previous height at line 470. Events beyond the limit in that same frame can be omitted. R01's partial-history floor fix is distinct and remains complete.                                                     | Owner defines lossless bounded paging using the canonical producer. Add a same-frame-over-limit regression without inventing a frontend cursor. Then verify exact event IDs/order, no omissions/duplicates and stale-page invalidation in `wallet-transactions.spec.ts` and `wallet/account/wallet-account-workspace.spec.ts`.                                                                   |
| B1 / W19                 | Retained report: Account admission rejects Lending. Existing `wallet/account/wallet-account-workspace.spec.ts:311` requires pool → loan → full repayment and attaches committed Runtime evidence at the first failure.                                                                                                    | Owner fixes/decides supported admission; preserve the positive test. Exactly selected Hub/asset and committed pool/loan/repayment must pass. Current failure does not authorize a new lending policy or removal of the feature.                                                                                                                                                                  |
| B3 / remote W10/W11      | `core/api/runtime-adapter/resolve.ts:956` omits `settlementWorkspace` from compact reads. `frontend/bridges/wallet/wallet-canonical-account-context.ts` uses the remote frame while embedded mode reads the exact live replica. Existing operator `settlement-evidence` returns counts/digests, not the approval payload. | Owner supplies an approved bounded read contract for the existing action. Frontend then validates/project it through current owners and proves remote peer approval → designated execution → chain finality. Retain proposer/non-executor, stale revision/hash, changed owner, cancel and duplicate guards. Extend `wallet-settlement.spec.ts`; local passing cases do not close the remote gap. |
| B8 / W17, W18c finality  | `wallet-transactions.spec.ts:228–238` observes `clear_requested` and awaits `cancelled`; `core/entity/tx/handlers/account/committed-input.ts:269` can throw `ACCOUNT_SWAP_CANCEL_SCOPE_UNRESOLVED:<orderId>`. Evidence: `output/plan-wallet-20260908/b8/`.                                                                | Authorized canonical cancellation fix plus first-divergence regression. Rerun exact cross-j positive flow and dock consumer; prove final `cancelled`, no duplicate command and consistent selected order/Runtime.                                                                                                                                                                                |
| B13 / npm/static cutover | `core/api/server/static-assets.ts:60` serves one root `index.html` with fallback; React has four `apps/<surface>/index.html` entries. `scripts/release/build-xlnfinance-package.ts:26` consumes `frontend/build` and filters Docs/LLM assets.                                                                             | Explicit static-serving scope authority before core edits. Share canonical route/verified-file semantics with the existing release-serving owner through an appropriate neutral boundary, not a Runtime-to-frontend implementation import. Pack and launch real npm artifact, prove all routes/assets/API/WS and changed-byte rejection.                                                         |

The B1/B2/B8 initial browser reproduction commands are the registered single-spec commands in the acceptance section with their exact paths. B3 is partly missing coverage, so the first task is to establish its remote test through the approved read contract, not claim an existing local test certifies it.

## C01b — complete consumer preparation before requesting cutover

**Ready:** update the review package without activating canonical commands. **Conditional:** B13 code changes require the separate scope above. Keep `docs/frontend/react-frontend-cutover-review.md` and its patch as review artifacts; ordinary explanatory documentation belongs in `docs/`.

| Consumer                                  | Required implementation and proof in the complete patch                                                                                                                                                                                                                                                                                                                                                                                                                      |
| ----------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Root/frontend commands and dev supervisor | `package.json`, `frontend/package.json`, `scripts/dev/{run-dev.ts,run-dev-child.sh,supervise-dev.ts}`: canonical delegates use React; one supervised four-root gateway on 8080; preserve backend readiness, capability/auth, API/relay WS and owned shutdown. Resolve the existing HTTPS listener deliberately. Restore an explicit verified-release preview command; do not silently remove a used preview workflow.                                                        |
| npm/server                                | `scripts/release/build-xlnfinance-package.ts` and authorized B13 owner: explicit release input; complete declared bytes/manifests; no old Docs filter or Runtime bundle fallback. Pack/launch the actual package and verify direct/nested routes, assets, API and owner access.                                                                                                                                                                                              |
| Native build consumers                    | `scripts/native/build-platforms.ts`, `frontend/capacitor.config.ts`, native shells and copied mobile projects: verified release → Wallet stage → disposable shell workspace. Replace implicit `frontend/build/index.html` probes and sanitization of verified inputs. Canonical `native:build` cannot keep an unmodified `cap sync` against the old webDir. Preserve signing/integrity and deep-link rules.                                                                  |
| Deployment                                | `scripts/deployment/deploy-platform.sh`: transfer complete prebuilt bytes, use canonical verification/activation/rollback and four-app route ownership; preserve edge/API/WS/CSP/auth. Remove production compilation and destructive replacement of the only rollback copy. Exercise an isolated target only.                                                                                                                                                                |
| Build/distribution CI                     | `.github/workflows/{build-and-test,distribution-release}.yml`: build once, publish one explicit release ID/directory as a job output/artifact, download/verify the same bytes per consumer; run full units and required browser/platform acceptance. Replace the draft patch's “verify every sha256-* directory” loop with the selected candidate. Do not choose by timestamp or rebuild separately per platform.                                                            |
| Retirement/tests/docs                     | Recompute the current 177-path candidate deletion list after I09; account for retained TS, static assets, generated-input producers and tests before deleting. Retarget useful tests and typed inventories from Svelte/coexistence owners to canonical React/shared owners. Document actual dev/build/check/preview/native commands under `docs/frontend/`; retire the misleading Svelte scaffold README content during cutover without creating new frontend documentation. |

**Review deliverable:** complete consumer diff, exact final candidate and manifests, retained/deleted file list, tests/commands for every changed consumer, and rollback identity with provenance. `git apply --check` is only a patch syntax/drift check. Prepare the full result before requesting C02 authorization; acceptance failures remain visible.

## Final acceptance — run against one final source and release

Run from the repository root unless marked **frontend**. First `git diff --stat 2d8050e3a747cd3605d320440cb5a9586ecbe9e9..HEAD -- frontend tests/frontend scripts/native scripts/release scripts/dev .github/workflows`; reread changed owners rather than treating ordinary drift as a stop. Preserve unrelated working-tree changes.

**Execution environment:** check `bun run stand:status` before heavy browser/scenario/platform runs. Hold the existing lock with `bun run stand:run --reason <task-id> -- <command>` when a runner does not acquire it. Current React runners do not. Use their isolated fixture ports/origins; never attach to or reset the user's live Runtime. Default process budget is 30 seconds. Arrange an explicit longer budget or approved CI/device run for work that cannot fit; a test's own timeout is not authorization. Poll the existing handle and stop its exact process group/children on timeout. Run heavy stands sequentially.

| Gate                            | Exact current command / owner                                                                                                                                                                                                                                                                                                                                                                                          | Acceptance                                                                                                                                                                                                                                                                                                                                 |
| ------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| V03 inventory                   | `bun test tests/frontend/tooling/frontend-shared-boundaries.test.ts tests/frontend/tooling/frontend-route-ownership.test.ts tests/frontend/tooling/frontend-capability-coverage.test.ts tests/frontend/tooling/frontend-platform-inventory.test.ts tests/frontend/tooling/frontend-parity-audit.test.ts tests/frontend/tooling/frontend-wallet-flow-audit.test.ts`; **frontend:** `bun scripts/checks/parity-audit.ts` | Every source/route/owner exists; descriptions and IDs match this plan. Keep registered coverage distinct from passing evidence. Do not mark financial/platform gaps complete by editing labels.                                                                                                                                            |
| V04 units/build/assembly        | `bun test tests/frontend`; **frontend:** `bun scripts/check.ts --all --level=frontend` after V04a (which subsumes the separate unit invocation)                                                                                                                                                                                                                                                                        | All units pass, all four checks/builds pass, release assembled once. Record source and generated-input provenance plus exact release ID; eliminate duplicate full-suite/build execution in final CI.                                                                                                                                       |
| V01/V02/V05a development matrix | **frontend:** `bun scripts/test-react-candidate.ts --all`. Narrow reproduction: `bun scripts/check.ts --surface=wallet --level=slice --spec=tests/react-candidate/wallet/account/wallet-account-workspace.spec.ts`; substitute registered `wallet/wallet-transactions.spec.ts`, `wallet/wallet-settlement.spec.ts` or Ops BrainVault path as appropriate.                                                              | All currently registered cases plus new regressions pass, no skipped positive flows. Include Site, Docs, public embed, shared Wallet/Ops, locale, real guide and Graph/history. Fix the first concrete new failure, not a speculative redesign.                                                                                            |
| V05b immutable interactions     | **frontend:** `bun scripts/release/candidate-release-verifier.ts <release-directory>`; `bun run test:react:artifact <release-directory>`                                                                                                                                                                                                                                                                               | All three viewports; direct/nested/Back/Forward routes, same-origin authority/storage, lazy assets, workers/downloads, real Ownership review/cancel/commit and recorded Graph/Solvency/Live. No source-module imports; before/after hashes identical.                                                                                      |
| V06/V08 lifecycle               | Explicit-release commands produced by V06a–V08a; current configs: `playwright.pwa.config.ts`, `playwright.deployment.config.ts`                                                                                                                                                                                                                                                                                        | Same release as V05b; PWA lifecycle and real isolated activation/corruption/rollback pass. Preserve the existing positive and rejection assertions.                                                                                                                                                                                        |
| V07 native staging/copy         | `bun scripts/native/stage-wallet-candidate.ts <release-directory>`; `bun scripts/native/copy-capacitor-shell-candidate.ts <stage-directory>`; `bun scripts/native/copy-packaged-shell-candidate.ts <stage-directory>`                                                                                                                                                                                                  | Verify copied bytes and release ID. Current contract command: `bun test native/__tests__/wallet-candidate-staging.test.ts native/__tests__/packaged-shell-candidate.test.ts native/__tests__/capacitor/capacitor-shell-candidate.test.ts native/__tests__/capacitor/capacitor-candidate-smoke.test.ts`. Then actual platform launch below. |
| V09 root                        | `bun run check` on final unchanged candidate                                                                                                                                                                                                                                                                                                                                                                           | Exit 0 with required toolchains; missing cargo, timeout or an interrupted child is incomplete. No frozen-core bypass. Earlier CI green on another SHA does not close this gate.                                                                                                                                                            |

The slice wrapper accepts only a registered `--spec`; it does not forward arbitrary Playwright flags. For a bounded viewport/spec run use **frontend** `PLAYWRIGHT_REACT_SURFACE=ops bunx playwright test --config playwright.react.config.ts tests/react-candidate/ops/panels/ops-workspace-brainvault.spec.ts --project=mobile-390x844`, under the root stand wrapper and approved budget. Use `wallet` for Wallet specs. Keep all three viewport results in final coverage; splitting execution is not permission to omit cases.

Record exact input SHA/release, command, exit, counts, duration, report and inspected screenshots/browser errors. A full dev-server pass cannot replace immutable-byte acceptance. Rebuild only after a source/generated-input change; then bind all affected evidence to the new release.

### Native/device acceptance still required

| ID             | Current evidence / environment limitation                                                                                                                                                                               | Required positive proof                                                                                                                                                                                                                                               |
| -------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| V07a iOS       | Prior disposable build resolved Capacitor dependencies but exceeded 30 seconds before producing an app. Xcode is present; prior inventory had 11 iOS 26.3 simulators. `output/react-finish-20260910/v07-ios-build.log`. | Recheck available simulator and run authorized build/launch from copied workspace. Deep links, background/resume, reload and persisted storage on exact candidate bytes. Record actual successful launch commands; do not claim copy-contract tests are device tests. |
| V07b Android   | Prior SDK 36/ADB absent; current PATH still has no `adb`.                                                                                                                                                               | Configured host/emulator/device, exact candidate build/install and equivalent lifecycle/deep-link/storage proof.                                                                                                                                                      |
| V07c desktop   | Prior Electron package lacked executable; bootstrap exceeded budget. `output/react-finish-20260910/v07-electron-bootstrap.log`.                                                                                         | Recheck binary, then real copied-shell launch, route/deep link, CSP, reload/storage, close/reopen and cleanup.                                                                                                                                                        |
| V07d extension | Real toolbar → Wallet settings → reload → close/reopen passed 3/3; extension `app.html` mapping is fixed.                                                                                                               | Rerun after changed Wallet/native bytes, preserving extension entry URLs and restrictions. Use `frontend/playwright.packaged.config.ts` with its required `PLAYWRIGHT_PACKAGED_DIRECTORY` and `PLAYWRIGHT_STAGING_DIRECTORY`.                                         |
| G04b WebXR     | Desktop lacked `navigator.xr`; prior simulator inventory had no visionOS. Supported-device acceptance has not happened.                                                                                                 | Real supported headset: enter/exit, controllers/selection/drag/double-tap/scale, panel close/session teardown and restored desktop scene/resources. Unsupported-state evidence does not close this row.                                                               |

Platform scope cannot be silently dropped to declare completion. Obtain a product scope decision if supported-device acceptance is to change; continue independent web work meanwhile.

## C02/C03 and definition of done

C02 begins only with explicit owner cutover authorization and a concrete reviewed consumer patch. Apply canonical command/routing/consumer changes and Svelte retirement as one coherent cutover. Preserve `frontend/static`, Docs producers such as `copy-static-files.js --docs-only`, contract assets and canonical workers. Remove Svelte-only dependencies/config/source, obsolete candidate placeholders/coexistence-only wiring with no consumer, and the temporary frontend override when the migration is merged. Regenerate affected lockfiles with Bun; do not hand-edit locks. Retarget useful assertions before retiring Svelte-specific ones.

The migration is complete only when all of these hold:

- [x] I09 resolved graph has zero retained-source imports from React/shared/bridge consumers and no Svelte runtime dependency; canonical helpers have one owner.
- [ ] R03 and W10/W11 remote, W17, W19, W20 and Site release positive flows have accepted evidence; B1/B2/B3/B8/B9 are resolved by authorized owners.
- [ ] Complete frontend units, required development browser matrix, final immutable interactions, PWA/rollback and supported native/headset cases pass with explicit provenance.
- [ ] Canonical dev/build/check/preview/npm/native/deployment/CI consumers use the reviewed release path; B13 production serving works; Svelte-only source/dependencies/config and temporary coexistence paths are retired.
- [ ] Final `bun run check` and changed-consumer gates pass, `git diff --check` is clean, and unrelated user work is preserved.

C03 is separately authorized production activation of the already built release. Before it, identify the actual deployed rollback release, verify transferred bytes, run applicable production smoke and demonstrate whole-release rollback. Never compile on production.

## Considered and excluded

No app rewrite, new router/state-management framework, UI redesign, speculative performance work, arbitrary file-size campaign, unreachable Architect Build mode, general core audit or new financial formulas is needed to finish this migration. Direct `src/lib` paths are ownership debt, not by themselves proof of bundled Svelte. The historical market-cap 504 and four sandbox socket failures are not established application bugs. Preserve prior successful implementation rather than repeating completed porting tasks.

This assessment covered migration intent, representative implementation owners, direct retained imports, unit/check/browser registration, stored release evidence and consumer wiring. It did not freshly validate protected protocol flows, every UI interaction, native devices, production deployment or the whole transitive import closure. I09 and final acceptance explicitly cover those remaining frontend checks.

**Next migration boundary:** The remaining explicit product/protected-owner decisions above. Ops independent-panel history now passes all three viewports; the full Ops matrix remains open. B9 is resolved. I09/V04a/V06a–V08a are implemented; keep full acceptance and cutover checkpoints open.
