# Нативный кошелёк xln для iPhone

Обновлено: 2026-09-13. Работа на main в общем изменённом дереве. Нативный SwiftUI-клиент
реализован и запущен в симуляторе; готовность к production **не подтверждена**.

По запросу владельца выполнен [трёхраундовый пересмотр 10 слабостей и GTM](xln-gtm-review-2026-09-13.md).
Рекомендация для первого пилота: один партнёр с существующей аудиторией, готовый
iPhone-клиент и ограниченная пара/сеть. Это предложение, не разрешение публикации
или реальных средств; текущие deliverables и release gates сохранены.

## Решение владельца: BrainVault и закрытый выпуск, 13 сентября

### Уточнение результата и метода работы, 13 сентября

После явно запрошенного владельцем независимого пересмотра двумя read-only
субагентами: recovery остаётся условием выпуска, но ожидание конкретного решения
по нему не блокирует независимую нативную торговлю. Следующий UI-результат — одна
пара USDC/WETH: живой стакан → своя цена/объём → открытый ордер → частичное исполнение
→ отмена остатка → точные итоговые суммы. Проверять на сохранённом тестовом device
wallet; затем повторить на восстановленном BrainVault. Эти два результата не смешивать.
После размещения открывать состояние именно этого ордера; в списках показывать краткие
суммы с полными значениями в деталях. Сохранить полный запрос владельца, не сокращать
выпуск до payment-only без его решения. После этих двух reviewers дальнейшая работа соло.

Техническое уточнение: вывод «нужно менять recovery checkpoint/commitments» ниже
слишком широк. Сначала исследовать явную каноническую Runtime-компактизацию допустимой
локальной истории с новым committed frame и прежним форматом snapshot/replay. Она
также требует точных правил signed-prefix/lock и rescan, не является уже одобренным
изменением протокола. Первый разрешённый эксперимент — диагностическая проекция на
изолированной копии источника: размер после rewind, неизменные Entity/Account roots,
ожидаемое отличие Runtime commitment. Проекцию не импортировать/не отправлять.
Измерить соответствие и tower 4 MiB, и API 1 MiB; не считать, что устранение первого
барьера устраняет второй. Перед запросом решения предъявить точную конструкцию,
стоимость повторного чтения цепи и условия отказа при подписанных обязательствах.

Владелец явно включил нативный ордербук в целевой iPhone-клиент вместе с платежами
и свопами. Текущий bridge использует живой `useOrderbook` и лучшую цену для свопа;
это не готовый торговый интерфейс. SwiftUI ещё не показывает глубину стакана,
не принимает выбранную пользователем лимитную цену и не управляет открытыми ордерами.
Проверенные same-J swap fills не засчитываются как проверка этих функций.

Рабочая единица — законченный пользовательский сценарий с доказательством, а не
час изменений. На каждой итерации: первый наблюдаемый дефект → минимальное исправление
общего production path → регрессия → повтор сценария через native UI. Отчёт различает
код, сборку, исполнение в Simulator и публичный testnet. Процент относится только
к явно названному набору проверок, не ко всему продукту. Новый видеопрогон записывать
после изменения поведения; не повторять прежний успех вместо закрытия дефекта.

Последовательность ближайших результатов:
1. Уменьшить recovery bundle по существующим правилам хранения подтверждённой истории;
   затем восстановить BrainVault на втором чистом симуляторе с совпадением identity,
   точных остатков и подтверждённых корней счетов. Не увеличивать квоты для обхода ошибки.
2. Довести нативный торговый путь через существующее финансовое ядро: живые bid/ask
   и глубина, цена и объём лимитного ордера, открытые ордера, отмена, частичное и полное
   исполнение. Проверить гонку отмены/исполнения по подтверждённому состоянию.
3. Проверить платёж и торговлю при обрыве связи/перезапуске, затем на публичном тестовом
   окружении с тестовыми средствами. Локальный Anvil не засчитывать как публичный testnet.

Цель «лучше Binance» — продуктовая планка, а не установленное сравнение. Для XLN
оценивать ясность доступных средств и обязательств, число действий до подтверждения,
видимость цены/комиссии/статуса и восстановление после сбоя. Сравнительные заявления
требуют одинаковых сценариев и наблюдений в обоих приложениях; таких измерений пока нет.

Первый выпуск — закрытый TestFlight с тестовыми средствами. Apple Developer Account
пока отсутствует; подпись и доставка TestFlight остаются внешним релизным условием.

Продуктовый критерий после сверки с архитектурой XLN: кошелёк — личный участник
сети расчётов с собственными ключами. Home должен различать on-chain средства,
резервы, требования к контрагентам (с обеспечением и без), собственный долг и
доступную платёжную ёмкость. Общая проекция `TokenTotals` уже содержит эти значения;
нынешний native bridge передаёт только net и sendCapacity. Следующее улучшение Home
должно раскрыть имеющиеся данные без второй финансовой формулы. Платёжный экран
показывает итоговое списание, получение, комиссию и состояние подтверждения.
Восстановление BrainVault со счетами остаётся первым незавершённым сценарием.

Ответы владельца, 13 сентября: максимально быстрый повседневный вход через Face ID;
удобный сценарий оплаты выбирает исполнитель; управление риском через лимиты
с готовыми значениями по умолчанию. Для оплаты выбрано единое намерение: получатель
задаёт сумму/актив, плательщик выбирает источник; необходимый обмен входит в
предложенные условия, итоговое списание/получение/комиссия видны до подтверждения.
Наличие такого маршрута и его завершение нужно доказать общим финансовым ядром;
текущие раздельные payment/swap E2E не являются этим доказательством.

Для Face ID после успешного открытия BrainVault нужно сохранить локальный ключ
в защищённом Keychain и открывать существующее состояние без повторного KDF.
Имя/пароль остаются способом восстановления на новом устройстве. Сохранение ключа
и быстрый повторный вход BrainVault пока не реализованы; прежний device wallet уже
использует Face ID. Проверка восстановления на втором симуляторе остаётся первой.

Для лимитов использовать каноническую политику юрисдикции, без новых iOS-констант.
Локальный API `/api/jurisdictions` сейчас публикует rebalancePolicyUsd:
`r2cRequestSoftLimit: 500`, `hardLimit: 10000`, `maxFee: 15`.
Это параметры обеспечения/ребалансировки; кредитные разрешения и их применение
должны оставаться в каноническом Account path. Перед переносом defaults в UI
проверить фактические пороги каждого счёта и токена, не считать USD-подпись
доказательством пересчёта для произвольного актива. Существующие лимиты счетов
в этом ответе не менялись; реальные средства не разрешены, выпуск закрытый тестовый.

Основной пользовательский путь — BrainVault: пользователь помнит пароль, вводит
свои прежние данные и восстанавливает кошелёк. Не требовать записи recovery phrase
или ручного экспорта файла как обязательной церемонии. Использовать существующий
канонический вывод из имени, пароля и параметров BrainVault; не менять алгоритм
или параметры существующей идентичности ради мобильного интерфейса.

До этой итерации iOS boot создавал случайную entropy в Keychain (`kind: mnemonic`);
показанные ранее платежи и обмены не доказывают работу BrainVault. Существующий тестовый
кошелёк и его историю сохранить: смена способа получения ключей не является
автоматической миграцией прежней идентичности.

Ближайший результат: подключить нативный ввод BrainVault к общему derivation и
recovery discovery, затем на втором чистом симуляторе доказать совпадение identity,
точных остатков и подтверждённого состояния счетов после входа теми же данными.
Пароль восстанавливает ключи; состояние счетов восстанавливается из проверенных
зашифрованных копий по [каноническому протоколу](recovery-watchtower-protocol.md).
Отсутствие recovery evidence должно быть явным, без создания пустого кошелька
вместо потерянного состояния. Ручная backup-форма — незавершённый промежуточный
инструмент; она не выполняет этот пользовательский критерий.

## Часовая итерация: нативный стакан и лимитная цена, 13 сентября, 08:41 UTC

Добавлен SwiftUI Market: живые bids/asks USDC/WETH из существующего `useOrderbook`,
цены/объёмы/спред и отметка своих уровней; вход с Home и из вкладки Market.
Котировки старше 30 секунд отключают выбор уровня. Выбор bid/ask открывает продажу/
покупку WETH, объём и редактируемую лимитную цену. `quoteOrder` использует общий
целочисленный `quoteForBase`, затем прежние `prepareSwapOrder`/`planSwap`/confirmation.
Показана эффективная лимитная цена и комиссия; рынок привязан к текущему hub.
На lock/crash/новом входе native market очищается. EN/RU строки добавлены.

Этап **3/4 (75%)**: подключение данных, интерфейс и сборки готовы; полный новый
торговый сценарий не прошёл. **8 tests pass / 28 assertions** (native amount, limit
amounts, shared best-level quote), UI tsc, Debug и Release build прошли.
В iPhone 17 Pro / iOS 26.5 проверены реальный стакан и изменение цены с 2500.5 на 2400:
для 0.001 WETH preview показал 2.4 USDC, minimum net 0.0009999 WETH, fee 0.0000001 WETH.
Затем отправлен один реальный тестовый ордер 0.001 WETH по 2500.5; hub закрыл его
на Account frame 25 с `below-minTradeSize`, без исполнения. Это НЕ успешный swap fill.
Остатки прежние 212.496203 USDC / 0.0149940004 WETH, pending=false, Account root прежний
`0x9d5346f0c4ba22b82ba4548dadc5c9fe73bedc32b7e331b52d3e77d063fc5665`.
Order id `swap-mtz8vcvt-y-0kb66qf50xiqf`. Не было повторной отправки/пополнения.

Первое падение: предварительная проверка цены не проверяет опубликованный minTradeSize
выбранного hub. Ядро корректно отклонило слишком маленький ордер; UI должен отклонять
его до отправки через общую проверку/политику, без новой iOS-константы. Следующий шаг —
найти авторитетный источник hub minTradeSize, добавить минимальную регрессию и повторить
именно этот сценарий. Управление открытыми ордерами, частичное исполнение и отмена
остатка ещё не реализованы/не проверены. RU/тёмная тема нового экрана пока не пройдены.

Артефакты: [стакан](../output/ios-market-2026-09-13/market-en.png),
[лимитная цена](../output/ios-market-2026-09-13/limit-review-en.png),
[причина отказа](../output/ios-market-2026-09-13/order-rejected.png),
[реальная запись](../output/ios-market-2026-09-13/market-proof.mp4).
Общий `check` снова остановлен `TEST_WORKSPACE_BUDGET_EXCEEDED`:
79,741,751,296 > 53,687,091,200 bytes. Новых Apple signing/TestFlight/18 Pro доказательств нет.
Первый simulator build ошибочно сделан без ad-hoc signing и получил Keychain -34018;
пересобран с прежними `CODE_SIGN_IDENTITY=- CODE_SIGNING_ALLOWED=YES`, данные сохранены.
Текущий установленный экземпляр — Debug; Release собрана отдельно.

Вывод по методу: независимый UI-сценарий дал новый видимый результат и конкретный
экономический отказ за один прогон. Не увеличивать тестовую сумму, чтобы замаскировать
отсутствующую проверку минимума. Следующее подтверждение — понятный отказ ДО отправки,
затем отдельный корректный ордер допустимого размера и точное исполнение.

## Часовая итерация: причина роста копии, 13 сентября, 07:40 UTC

Реальный исходный BrainVault открыт повторно без нового пополнения. Копия после
gzip/AES-GCM: **26,468,699 bytes**. Во второй юрисдикции подтверждённый Entity J anchor
стоит на высоте 2050, локальная непрерывная история дошла до 329730. Между ними
**327,681 blockHashes**, **0 eventBlocks**, размер jHistory **25,453,530 bytes**.
Штатный `pruneFinalizedValidatorJHistory` оставляет те же байты: история лежит ПОСЛЕ
подтверждённого anchor. Гипотеза «экспорт забыл удалить уже подтверждённую историю»
для этого дефекта опровергнута. У основного Entity также нет сокращения:
finalized 403384, scanned 406995, 3612 hashes, 282048 bytes.

Причина подтверждена кодом: `hasDueLocalJPrefixAdvance` намеренно не создаёт Entity
frames для пустых блоков. Их headers остаются validator-local до следующего реального
Entity input. `assertValidatorJHistoryIntegrity` требует каждый header непрерывного
суффикса, а recovery replay сверяет `replicaMetaDigest`, в который входит эта история.
Поэтому удаление headers из одного лишь backup нарушит каноническое восстановление.
`rewindValidatorJHistory` существует для явного reorg Runtime input; переносить его
в exporter нельзя без решения о семантике восстановления и сохранности подписанного
J-prefix/lock evidence. Автоматически создавать фиктивные финансовые операции ради
сокращения копии также нельзя.

Проверки текущего этапа **2/4 (50%)**: реальная диагностика размера и границы finality;
существующие регрессии J history — **33 pass, 0 fail, 99 assertions**.
Исправление production path и повтор в Simulator
не выполнены: обнаружен реальный выбор протокола. Новых изменений приложения нет,
сборка прежнего кода и заблокированный общим размером workspace check не повторялись.
Доказательства: [history evidence](../output/ios-brainvault-restore-2026-09-13/source-history-evidence.json).
Исходный browser context закрыт, профиль и финансовое состояние сохранены.

Предлагаемый выбор для владельца: изменить общий канонический recovery checkpoint
так, чтобы повторно считываемая история J после подтверждённого anchor не раздувала
переносимую копию. Это требует согласованного изменения WAL/replay commitments,
явных правил для ещё не применённых событий и подписанного prefix evidence,
а также проверки точных корней и выходов после восстановления. Не делать iOS-only
формат и не ослаблять сравнение digest. До решения core и квоты не менять.

Вывод по методу: измерение поля без его положения относительно finality привело бы
к неверному исправлению. Теперь первый шаг — проверять не только размер, но и
обязательства восстановления. Следующий конкретный шаг после решения — минимальный
production regression для idle secondary jurisdiction с длинным header-only suffix.

## Часовая итерация: предел размера копии, 13 сентября, 06:38 UTC

Положительное восстановление остановилось **до импорта на второй iPhone**.
На настоящем локальном стенде создан BrainVault `ios-funded-brainvault-20260913`
(factor 1), получены ровно 100 тестовых USDC. Runtime
`0xa032105c797cc6ff14d1b15ca0952e47f746e292`, Account height 4,
root `0x1eff21364aa3c5c00c7de7d25cd32f413cfd6e8a625a4236cf5855bd2b56fc74`,
pending=false, mempool=0. Повторные диагностические открытия не пополняли его снова.

Реальная копия после штатного gzip/AES-GCM занимает около 26.2 МБ, предел сервиса —
4,194,304 bytes. PUT 26,191,166 bytes вернул HTTP 500 с пустым не-JSON ответом;
локальный API дополнительно имеет предел тела 1 МБ. Поле `jHistory` второй Entity
заняло 25,453,530 bytes в канонической сериализации; её собственное state — 3,138 bytes.
Удалять историю или повышать квоты ради успешного теста не стали. Связь этой истории
с подтверждённым J anchor и существующим pruning ещё требуется проверить.

Исправлено поведение при отказе: общий `backupToTowers` читает заявленный предел
сервиса и не отправляет копию, которая заведомо не помещается даже без служебных
данных сервера. Положительный результат этой проверки не заменяет серверную квоту
и не объявляет копию сохранённой. Отсутствующий/невалидный предел отклоняется явно.
Повреждённый HTTP-ответ теперь сохраняет в сообщении его статус. iOS показывает
RU/EN сообщение с размером копии, лимитом и фактом, что копия не отправлена.
Удалён дублирующий health-запрос из native wrapper.

Проверки исправления **4/4 (100% этого ограниченного этапа)**: 3 unit tests / 8 assertions;
проверка типов и Debug/Release; реальная браузерная копия 26,223,467 bytes отклонена
при лимите 4,194,304 с **0 PUT-запросов**; на iPhone 17 Pro / iOS 26.5 показана
реальная копия 32.9 МБ и лимит 4.2 МБ без ложного успеха. Остатки прежнего кошелька
на Home сохранились: 212.496203 USDC и 0.0149940004 WETH.
Общий check: `TEST_WORKSPACE_BUDGET_EXCEEDED`, 77,789,712,384 > 53,687,091,200 bytes.

Артефакты: [ошибка на iPhone](../output/ios-brainvault-restore-2026-09-13/capacity-native.png),
[видео](../output/ios-brainvault-restore-2026-09-13/capacity-proof.mp4),
[размеры частей копии](../output/ios-brainvault-restore-2026-09-13/source-size-evidence.json),
[счёт и отказ до отправки](../output/ios-brainvault-restore-2026-09-13/capacity-evidence.json),
[HTTP evidence](../output/ios-brainvault-restore-2026-09-13/capacity-network.json).

Вывод по методу: в Vite импорт `adapter.ts` без текущего HMR query создавал другую
module instance с null env; диагностический тест теперь берёт активный runtime из
существующего debug handle. После первого HTTP-сбоя измерены статус и байты,
затем размеры полей канонической копии; повторять отправку без этих данных бессмысленно.
Размеры считаются без вывода расшифрованных данных или ключей в артефакты.

Для восстановления создан чистый iPhone 17 Pro Simulator `XLN BrainVault Restore`,
UDID `2830FDD6-AD1E-4072-842F-A8B632DD01E1`; после выявления барьера выключен,
данные не стирались. Исходный browser profile сохранён в
`/tmp/xln-ios-brainvault-source-profile`. **Счета на втором устройстве не восстановлены.**
Следующий шаг: сопоставить jHistory с lastFinalizedJHeight и каноническим
`pruneFinalizedValidatorJHistory`, доказать допустимую компактную копию теми же
корнями/выходами, затем повторить импорт. Apple signing и iPhone 18 Pro остаются
отдельными непроверенными условиями выпуска.

## Часовая итерация: нативный вход BrainVault, 13 сентября, 05:37 UTC

Добавлена SwiftUI-форма имени/пароля с прежним factor или custom shard count.
Она вызывает общий `deriveBrainvaultMnemonic` и каноническое recovery discovery:
на новом устройстве кошелёк открывается только после получения проверенной копии;
существующее локальное состояние открывается общим boot. Создание случайной entropy
при входе удалено. Уже существующий Keychain-кошелёк доступен отдельной кнопкой;
ключи и данные не перезаписаны. Пароль не сохраняется новым кодом, не выводится в
логи и очищается из поля после отправки/закрытия/ухода в фон. Отмена завершает workers.

Проверено **4/4 выбранных сценария (100% этого этапа, не готовности релиза)**:

1. Браузерный worker и iPhone 17 Pro / iOS 26.5 вывели одинаковый публичный Runtime id
   `0xb60405a0d5ef0ac5f1c1a7454852bf28a99fa2ee` из одних тестовых данных, factor 1 / 1 shard.
2. При отсутствии копии реальный tower отвечает без кандидата; нативный UI явно
   отказывает. Новый Playwright regression дополнительно доказал неизменность списка
   IndexedDB и отсутствие запущенного runtime: **1 test passed, 4 assertions**.
3. Отмена factor 3 / 100 shards возвращает форму без кошелька; следующий вход работает
   до того же recovery-барьера. Поле пароля очищено. Полный factor 3 также дошёл до
   поиска копии, но его межплатформенная идентичность в этом этапе не сравнивалась.
4. После входа Face ID в прежний кошелёк точные entity, остатки, Account height/root
   совпали с предыдущим экспортом: 212.496203 USDC, 0.0149940004 WETH, height 23,
   pending=false. Финансовых операций в этой итерации не выполнялось.

Debug и Release собраны, Release установлен. Проверка типов прошла. RU-текст ошибки,
закрытие формы без повторного alert и отмена проверены через native UI.
Общий check всё ещё блокирует `TEST_WORKSPACE_BUDGET_EXCEEDED`:
77,719,130,112 > 53,687,091,200 bytes при первом финальном запуске. Лимит не менялся.
Повторный check после финальной правки прогресса: 77,443,182,592 > 53,687,091,200 bytes, та же причина.
На машине есть runtime iOS 27, но доступного профиля iPhone 18 Pro нет;
эта итерация проверялась только на iPhone 17 Pro / iOS 26.5.

Артефакты: [видео отмены и отказа восстановления, 1:28](../output/ios-brainvault-2026-09-13/brainvault-proof.mp4),
[русская ошибка](../output/ios-brainvault-2026-09-13/no-backup-ru.png),
[идентичность и сохранность кошелька](../output/ios-brainvault-2026-09-13/native-evidence.json).
Видео — реальная Debug-запись без подмены, приведённая к 25 fps; в финальной Release
дополнительно переведена кнопка Cancel и сбрасывается завершённый текст прогресса.

Вывод по методу: сначала сравнение публичной идентичности и реальный отказ без backup
дали проверяемую границу без создания нового финансового состояния. Первую попытку
нажать Cancel инструмент пропустил после окончания вычисления; связка старт → свежий
AX → Cancel в одном вызове воспроизводимо проверила отмену. Чтение всего кошелька
после Face ID подтвердило сохранность, одной успешной сборки для этого недостаточно.

Следующий конкретный шаг: получить каноническую зашифрованную копию тестового BrainVault
с ненулевыми счетами и восстановить её на втором чистом симуляторе; сравнить identity,
суммы и подтверждённые корни, затем выполнить один тестовый платёж. **Положительное
восстановление счетов и обычное повторное открытие BrainVault пока не доказаны.**
Создание нового BrainVault и автоматическое поддержание свежести копий в iOS также
остаются следующими шагами. Apple Developer Account пока отсутствует.

## Часовая итерация: восстановление после WebKit crash, 13 сентября

Воспроизведён конкретный дефект: завершение процесса WebContent оставляло старые
балансы и активные финансовые кнопки SwiftUI. Теперь callback завершения сначала
снимает авторизацию native UI, очищает snapshot/notice и закрывает прежние WebSocket,
после чего сохраняется штатная перезагрузка Capacitor. Новый вход требует Face ID;
пользовательские финансовые намерения автоматически не повторяются. Ответы от старого
поколения runtime отвергаются с понятным RU/EN сообщением. Поздние callbacks старого
socket task не доставляются в новую JS-сессию.

Проверено **2/2 сценария** через реальное завершение WebContent в iPhone 17 Pro / iOS 26.5:
сбой открытого кошелька и сбой во время read-only экспорта durable receipts. В обоих случаях
кнопка нового входа доступна, старый баланс скрыт, Face ID возвращает прежнее состояние.
Второй сценарий дополнительно сравнен автоматически по полному before/after snapshot:
entity, raw token balances, Account height/root совпали; pending=false. Закрыты 3 сокета.

Артефакты: [видео восстановления, 55 секунд](../output/ios-runtime-recovery-2026-09-13/recovery.mp4),
[точное сравнение](../output/ios-runtime-recovery-2026-09-13/evidence.json),
[исходный дефект](../output/ios-runtime-recovery-2026-09-13/before.mp4).
Hook использует публичный [WKNavigationDelegate callback](https://developer.apple.com/documentation/webkit/wknavigationdelegate/webviewwebcontentprocessdidterminate(_:));
остальная навигационная политика передаётся прежнему делегату Capacitor.

Вывод по методу: реальный сбой процесса показал две разные границы — устаревший UI и
поздний ответ незавершённого вызова. Исправлена одна связанная причина, а не весь recovery
слой. При проверке сумм сначала ждать готового экрана, не трактовать экран загрузки как
финансовое расхождение. Потеря процесса во время незавершённого **платежа**, потеря
устройства и восстановление по backup по-прежнему не доказаны. Следующий шаг — recovery
на другом устройстве с существующим каноническим форматом backup, без нового протокола.

Debug/Release builds и `git diff --check` прошли. Итоговый ролик 54.875 с полностью
декодируется без ошибок. Обязательный `bun run check` завершился прежним барьером:
workspace **77,131,640,832 > 53,687,091,200 bytes**. Общая релизная готовность не заявляется;
чужие данные не удалялись, лимит не менялся. Финальная Release установлена в симуляторе
и сохранена в `output/ios-runtime-recovery-2026-09-13/xln-simulator.app`.

## Последний результат: видео E2E, 13 сентября

[Видео 5:30 и подробное покрытие](ios-e2e-video-2026-09-13.md): 12 выбранных сценариев,
реальный платёж 25 USDC, обмены USDC → WETH и WETH → USDC, QR, история, настройки,
Face ID, блокировка, RU/EN и обе темы. Исходные записи сохранены. Финальные остатки:
212.496203 USDC + 0.0149940004 WETH; Account 23, pending/mempool/offers = 0.
Канонический export подтвердил ровно один новый платёж и два новых swap fills.
Release: `output/ios-e2e-video-2026-09-13/xln-simulator.app`.

В процессе прогона улучшены выбор двух активов (прямые нативные стеклянные кнопки) и
переход от подключения H1 на Home к подтверждённому состоянию. 26 тестов / 93 assertions
прошли; общий check по-прежнему блокирует workspace budget (77.3 ГБ против 53.7 ГБ).

Пересмотр метода: записывать короткими главами с корректным SIGINT под machine lock;
отдельно хранить сырой ролик и финансовые receipts. Ограничения AX-инструмента не считать
провалом протокола или доказательством полной доступности. Следующая полезная итерация —
восстановление на другом устройстве; не наращивать косметику вместо этой релизной границы.

## Направление, выбранное владельцем

- Русский и английский автоматически по языку iPhone; числовые разделители — по региону.
- Системные светлая/тёмная темы, сдержанный Liquid Glass, SF Symbols, системная навигация.
- Целевое устройство — iPhone 18 Pro. Фактически финансовые сценарии проверены на
  iPhone 17 Pro, iOS 26.5, Xcode 26.6 (17F113). Эти устройства не приравниваются.
- iOS 27.0 Simulator (24A434) установлен. Для профиля iPhone 18 Pro нужен Xcode 27 RC;
  его загрузка требует входа владельца в Apple Developer. Xcode 27 ещё не установлен.

## Что реализовано

В существующем проекте `frontend/ios/App/App.xcodeproj`, bundle ID `finance.xln.wallet`,
видимый интерфейс заменён на SwiftUI: Home, Activity, Manage, Settings, платёж, получение
с QR/Share Sheet, USDC ↔ WETH same-J swap, явное получение тестовых средств.
История читает страницы канонического API; отдельно показаны исполнения, комиссии и
причины закрытия ордеров. Приём команды не называется подтверждённым платежом.

SwiftUI получает точные строковые суммы и проекции от того же локального TypeScript
runtime, что использует веб. Упакованный WKWebView служит невидимым host; это **нативный
UI с общим JS-ядром**, не новый Swift consensus engine. URLSession реализует транспорт
WebSocket; подписи hello, шифрование, ACK, WAL и финансовые переходы остаются в ядре.

Ключевой материал создаётся системным генератором и хранится в Keychain с
`WhenUnlockedThisDeviceOnly`. Вход требует Face ID или код-пароль. При уходе в фон UI
блокируется; runtime закрывается после завершения уже выполняющейся команды.
Privacy cover закрывает всё окно, включая модальные экраны. Экспорт/импорт recovery phrase,
проверка signing-boundary и восстановление на другом устройстве ещё не реализованы/не доказаны.

Старые web-плагины управления статусной строкой, клавиатурой и splash исключены из
регистрации iOS: эти области принадлежат SwiftUI. Веб- и Android-клиенты сохраняются.

## Предыдущие проверенные результаты, 12 сентября

| Проверка | Результат |
| --- | --- |
| Локальный runtime в упакованном приложении | Запуск, Web Crypto, worker, lock, Keychain — работают |
| Получение тестовых средств | Реальные 100 USDC через локальный H1 |
| Платёж H2 | Получателю 25 USDC, фактический debit 25.000025, fee 0.000025 USDC |
| Защита от двойного нажатия | В durable receipts ровно 1 HtlcInitiated и 1 HtlcFinalized с одним hashlock |
| Исполненный swap | Отдано 24.999999 USDC, gross 0.009998 WETH, fee 0.0000009998 WETH, net 0.0099970002 WETH |
| Закрытие ниже минимума | 9.996999 USDC: canonical comment `below-minTradeSize:9996999`; исполнение отсутствует, балансы не изменились |
| Перезапуск/переустановка без удаления данных | Те же точные остатки, Account height 12 и root; pending=false, mempool=0, offers=0 |
| Возврат из фона | Повторный защищённый вход; предыдущий баланс скрыт |
| Русская локаль | Переводы и точные разделители сумм проверены на экране |
| Светлая/тёмная тема | 4/4 Home: RU/EN × светлая/тёмная; screenshots в output |
| Debug / Release Simulator | Обе сборки успешны; Release установлен, Face ID и прежние остатки проверены, debug controls отсутствуют. Это не iPhone/TestFlight-подпись |
| Целевые проверки кода | 26 passed, 0 failed, 93 assertions в 7 файлах |
| Общий веб-клиент | HTTP 200, стартовый экран, 0 page errors, отдельный чистый browser context |
| `bun run check` | FAILED: TEST_WORKSPACE_BUDGET_EXCEEDED; workspace 72,756,232,192 > cap 53,687,091,200 bytes (budgeted subset 57,224,157,121) на момент проверки |

Это локальный стенд с настоящим runtime, H1/H2 и Anvil. Подтверждения настоящих операций
не означают mainnet-проверку или использование реальных денег. Стенд не является измерением TPS.

Артефакт: `output/ios-native-2026-09-12/xln-simulator.app` (18 МБ, требует локальный стек).
Точные доказательства: `output/ios-native-2026-09-12/financial-evidence.json`; сборка и её hash — `build-evidence.json` в той же папке.
Account root: `0xe369ccc1cea0481dbcd9592eeaffa022c5802d7a116b1c988e5defee9581bc00`.
Остатки: USDC raw `49999976`, WETH raw `9997000200000000`.

## Воспроизведение

Нужен работающий локальный стек. Для текущего стенда API находится на 127.0.0.1:8082;
публичный локальный вход остаётся localhost:8080. Порт API не является новым UI entry point.

```sh
XLN_UI_STACK_ORIGIN=http://127.0.0.1:8082 bun scripts/native/session/build-ios-runtime.ts
xcodebuild -project frontend/ios/App/App.xcodeproj -scheme App \
  -configuration Debug -destination 'platform=iOS Simulator,name=iPhone 17 Pro' \
  -derivedDataPath /tmp/xln-ios-derived CODE_SIGN_IDENTITY=- CODE_SIGNING_ALLOWED=YES build
```

Ad-hoc signing в симуляторе необходимо для Keychain. Не отключать entitlement/signing,
не стирать данные и не заменять балансы заглушками при ошибке. `--no-build` требует
совпадения настроенной сети с упакованным host, чтобы случайно не переиспользовать локальный
артефакт для production. Без переменной origin целевая сеть — https://xln.finance.
Это значение ещё не доказывает работоспособность удалённого мобильного клиента.

Для финансового E2E: `bun run stand:status`, затем существующий machine lock.
Для повторного экспорта доказательств в Debug: Settings → Export test evidence.
Экспорт не содержит seed, private keys или HTLC secrets. Release-кнопки экспорта нет.

## Первый барьер выпуска

На машине нет действующих Apple codesigning identities, DEVELOPMENT_TEAM не настроен.
Не создан signed device archive, IPA, App Store Connect record или TestFlight build.
Для фактической проверки iPhone 18 Pro также нужен Xcode 27. Не подменять эти gates
симуляторной сборкой и не называть отсутствие ошибок сборки полной корректностью.

Общий check блокирует лимит тестового workspace. Владелец должен определить, какие
старые данные стендов можно удалить/перенести; чужие данные не удалялись, лимит не повышался.

## План до production

1. **Воспроизводимый выпуск:** Xcode 27, iPhone 18 Pro, Apple team/certificate; зелёный
   `bun run check`, signed archive/export/install. Сначала исправлять первый упавший gate.
2. **Безопасное владение:** BrainVault из запоминаемых данных, автоматическое получение
   и проверка зашифрованного состояния, восстановление на другом iPhone,
   реальные Face ID cancel/deny, passcode removal, проверка lifecycle при незавершённом платеже,
   termination WebContent process, airplane mode/reconnect и отсутствие повторной команды.
3. **Полнота продукта:** смена Entity, cross-J swap, Move, withdrawal/deposit, settlement/dispute,
   лимиты/обязательства; сканер адресов, deep-link intent, APNs. Реальные средства запрещено
   считать готовыми только потому, что local same-J сценарий прошёл.
4. **Дизайн и доступность:** оба языка × обе темы × Pro/Pro Max, максимальный Dynamic Type,
   VoiceOver, Reduce Motion/Transparency, Increase Contrast, длинные суммы, ошибки и пустые состояния.
   У Simulator CUA AX есть ограничение: системные toolbar/tab items отсутствуют в дереве;
   полный VoiceOver результат на основании этого инструмента не заявляется.
5. **Устройство/TestFlight:** измерить cold start, scrolling/hitches/ProMotion, восстановление,
   энергопотребление; установить через TestFlight. Публикация в App Store — отдельный факт.

Известное улучшение: backend не публикует minTradeSize в клиентском market descriptor;
клиент честно показывает каноническую причину закрытия ниже минимума. Нельзя подменить
этот контракт захардкоженной второй политикой размера сделки.

«1000/1000» — пожелание к качеству. Измеренного рейтинга или гарантии идеальности нет.

Apple: [Liquid Glass](https://developer.apple.com/documentation/TechnologyOverviews/adopting-liquid-glass),
[iPhone 18 Pro](https://www.apple.com/newsroom/2026/09/apple-debuts-iphone-18-pro-and-iphone-18-pro-max/),
[Xcode requirements](https://developer.apple.com/xcode/system-requirements).

## Три кредитных хаба: выполненный платёж из SwiftUI, 13 сентября

[Показ и точные доказательства](three-hub-stage-2026-09-13.md).
В этой итерации закрыт платёж через H1 → H2 → H3: списание 25.000075 USDC,
получение 25 USDC, комиссия 0.000075 USDC. Нативный экран позволяет выбрать
реальный маршрут и заново получить условия. После холодного запуска через Face ID
остаток и подтверждённый Account frame 27 сохранены. Это сохранённый device wallet,
не fresh-device BrainVault recovery. 77 связанных тестов прошли, Debug/Release
собраны; полный check блокируется workspace budget. Исходный блокер minimum trade
и незавершённый цикл лимитного ордера остаются открытыми.

## Ошибка памяти IndexedDB: исправлена причина разрастания, 13 сентября

[Разбор и видео](ios-storage-recovery-2026-09-13.md). Срезы общего буфера заставляли
WebKit сохранять 8.2 МБ на каждый фрагмент в 9 КБ. Новые фрагменты владеют своими
байтами; чтение одного значения ограничено одним фрагментом за раз. Формат,
ключи и проверки целостности сохранены. После установки поверх прежних данных
кошелёк восстановился до Account 29 без pending/holds и без изменения остатков.
22 теста прошли, Debug/Release собраны; общий check по-прежнему блокирует quota.

Метод изменён по фактам: автоматическое открытие полной истории после отправки
ордера отменено. Нейтральный статус и offerId сохранены, история открывается
отдельным действием. Следующая котировка всё ещё задержалась при обработке
истории; это следующий production blocker, а не повод добавлять дизайн или
объявлять выпуск. Следующее действие — локализовать блокировку и повторить
полный цикл нового ордера с видео; свежие средства для этого не нужны.

## Котировка использует тот же снимок, что React, 13 сентября, 11:42 UTC

Удалено повторное чтение Account перед обычной нативной котировкой. Теперь
`commands.ts` использует `hub.doc.state` из подтверждённого detached ViewFrame,
как React Swap. Тип `SwapParty.account` соответствует входу канонического
`planSwapCommand`, без приведения DTO к внутренней persistent collection.
После явной подготовки входящего кредита остаётся свежее чтение подтверждённого
счёта; готовность и срок рыночных данных проверяются до выдачи нового ticket.

Результат: **4/4 ручных сценария котировки (100%)** в iPhone 17 Pro / iOS 26.5:
RU покупка/продажа, EN покупка/недостаточный входящий лимит. Все показали условия
без прежнего зависания. В последнем случае нет подтверждения ордера, разрешение
тестового кредита выключено. Счёт остался на height 29, pending/mempool=0;
остатки и кредитные лимиты совпали с состоянием до теста. Ордеров отправлено **0**.

[Видео RU](../output/ios-quote-snapshot-2026-09-13/quote-proof-ru.mp4),
[видео финальной сборки EN](../output/ios-quote-snapshot-2026-09-13/quote-proof-en.mp4),
[скрин условий](../output/ios-quote-snapshot-2026-09-13/buy-review.png),
[проверка сумм и счёта](../output/ios-quote-snapshot-2026-09-13/verification.json).
Покупка: 12.5025 USDC → минимум 0.0049995 WETH, комиссия максимум 0.0000005 WETH.
Продажа: 0.005 WETH → минимум 12.496251 USDC, комиссия максимум 0.001249 USDC.
Суммы независимо сверены с каноническими функциями nativeLimitAmounts и
deriveSwapNetAuthorization, а неизменность счёта — с ответом H1.

Проверки: **15 pass / 0 fail / 45 assertions**, UI TypeScript, native host,
Debug и Release прошли. `bun run check` выполнен и остановлен прежним
`TEST_WORKSPACE_BUDGET_EXCEEDED`: workspace 82,190,348,288, max 53,687,091,200 байт.
Активный Xcode 26.6, тест выполнен на iPhone 17 Pro; iPhone 18 Pro не проверен.

Сработало удаление дублирующего чтения уже опубликованных данных. Восстановление
истории остаётся дорогим и может задерживать запись кадров; эта итерация не
доказывает его исправление, исполнение ордера или live-подготовку нового кредита.
Следующий шаг — локализовать задержку writer за историческими чтениями и провести
один полный тестовый обмен с подтверждёнными остатками и видео.

## 2026-09-13 — дефолтный сервис восстановления без ввода URL

По запросу владельца удалены поля адреса из SwiftUI BrainVault и резервной копии,
а также адрес из команд presentation bridge. Оба пути используют одну конфигурацию:
`VITE_XLN_WATCHTOWER_URL`, по умолчанию https://xln.finance; для локальной HTTP-сборки
наш watchtower http://127.0.0.1:9100. Сборщик сохраняет recoveryUrl вместе с apiBase
и отклоняет повторное использование ресурсов с другой конфигурацией.
Сообщение об отсутствии копии больше не просит пользователя проверять сервис.

Проверки: 4/4 теста политики адресов, 12 assertions; UI TypeScript, native host и
финальная Debug-сборка прошли. Установлена сборка для API8082/tower9100.
В iPhone 17 Pro / iOS 26.5 проверены 2/2 экрана без URL. Новая тестовая BrainVault
учётная запись прошла локальную деривацию и поиск копии с дефолтным адресом;
получен ответ об отсутствии проверенной копии. Это не успешный restore.
Сохранённый кошелёк открыт через тестовую биометрию; экран копии не содержит поля
адреса. Платежи, ордера и загрузка новой копии в этой проверке не выполнялись.

Скриншоты: `output/ios-default-recovery-2026-09-13/brainvault-default.png` и
`output/ios-default-recovery-2026-09-13/backup-default.png`. Первый снят до последней
правки текста ошибки. Логи сборки/проверки: `/tmp/xln-ios-default-recovery-*.log`.
Полный `bun run check` заблокирован TEST_WORKSPACE_BUDGET_EXCEEDED; успешная сборка
не означает прохождение общего gate. Существующий блокер восстановления другой
копии (26 MB против лимита сервиса 4 MiB) этим изменением не исправлен.

## 2026-09-13, 12:43 UTC — сетевой сбой восстановления не означает неверный пароль

Завершён один независимый шаг после повторного чтения установленного блокера
26 MB: общий UI использует существующие категории discovery ExpectedEmpty,
TransientRace и Contradiction. Только ожидаемое отсутствие копии предлагает
проверить данные BrainVault; сбой подключения предлагает повторить позже;
непроверенные данные отклоняются без импорта. Исходные диагностические ошибки
сохранены. Native backup использует тот же обработчик, каноническая проверка
кандидатов и формат восстановления не изменены.

Настоящий отказ подключения в iOS WebKit вернул `Load failed`; прежний общий
классификатор ошибочно считал его Contradiction. Добавлено точное распознавание
этого сообщения как TransientRace. Первая версия Swift-локализации выявила
рекурсию при обработке точного ключа; заменена прямым lookup Bundle, добавлена
исполняемая Swift-регрессия для точного ключа и диагностического суффикса.

Проверки: **8/8 Bun tests, 25 assertions**, **5/5 Swift assertions**,
**1/1 браузерный E2E** (3.7s, реальный tower, без ввода URL, после отказа базы
не создаются и runtime не запускается), TypeScript, native host, финальный Debug.
**2/2 сценария Simulator (100%)**: RU реальный сетевой отказ на недоступном
HTTPS loopback endpoint; EN ожидаемое отсутствие копии через настоящий tower9100.
Для сетевого теста использовалась обычная конфигурация сборки, без мок-сервера.
Финальные ресурсы и установленная сборка возвращены на API8082 / tower9100.
Устройство: iPhone 17 Pro / iOS 26.5, iPhone 18 Pro не проверен.

[Сетевой отказ RU](../output/ios-recovery-errors-2026-09-13/network-error-ru.png),
[нет копии EN](../output/ios-recovery-errors-2026-09-13/no-backup-en.png).
Frozen core unchanged. `bun run check` выполнен: прежний
TEST_WORKSPACE_BUDGET_EXCEEDED, workspace 81,515,225,088 bytes,
max 53,687,091,200. Квоты и данные не изменялись, повторного запуска этого gate нет.
Успешное восстановление второго iPhone и устранение большой копии не доказаны.

Вывод по методу: проверка реального WebKit обязательна — браузерные строки ошибок
отличаются от Chromium, а компиляция не ловит рекурсию локализации. Повторно
пытаться загрузить 26 MB без изменения причины бессмысленно. Следующий шаг:
получить решение владельца о каноническом recovery checkpoint для длинного
нефинализированного header-only суффикса; до него не обрезать committed evidence.

## 2026-09-13 — нативный вход по дизайну frontend

Владелец отклонил самостоятельный редизайн и указал брать существующий frontend.
Финальный источник: RuntimeCreation.svelte, runtime-creation.css и точный
frontend/static/img/logo.png, скопированный в XlnMark.imageset. Эталон реально
открыт в отдельном браузерном контексте на https://localhost:8080/app, снимок
output/ios-login-design-2026-09-13/frontend-reference.png. Общую пользовательскую
вкладку и её active lock не сбрасывали.

SwiftUI вход использует знак xln, карточку, исходные светлый indigo #4f46e5 и
тёмные тёплые акценты, два поля и свёрнутые дополнительные параметры. Убраны
несколько крупных Form-секций и дублирующие инструкции. Liquid Glass оставлен
на действиях; настройки открываются нативным листом. Имя, секрет и параметры
передаются прежнему on-device TS/BrainVault пути, адрес сервиса не вводится.

Ручная проверка **4/4 группы сценариев (100%)**: RU light, RU/EN dark,
показ/скрытие секрета и настройки без потери ввода, программная клавиатура и
отправка формы. Поле пароля прокручивается в видимую область при уменьшении
экрана клавиатурой. Для публичной тестовой записи с 6 shards реально выполнены
деривация и поиск через tower9100; ожидаемый результат — проверенной копии нет.
Это не положительный restore. В раннем варианте замена SecureField вызывала
системный Save Password; сохранены стабильные поля с доступностью только
видимого. Повторное переключение прошло без диалога. Контраст текста основной
кнопки отдельно исправлен для золотого фона тёмной темы.

Финальная Debug-сборка собрана и установлена; в симуляторе оставлен RU light
экран входа без введённых секретов. iPhone 17 Pro / iOS 26.5; iPhone 18 Pro и
полный проход VoiceOver/Dynamic Type не проверены. Изменений финансового ядра
в этой итерации нет. `bun run check` запускался и снова остановлен общим
TEST_WORKSPACE_BUDGET_EXCEEDED: workspace 88,147,427,328, max 53,687,091,200 bytes.

Артефакты: login-light-ru.png, login-dark-ru.png, login-dark-en-ready.png,
keyboard-en.png, recovery-result-en.png в output/ios-login-design-2026-09-13.
Последний cosmetic fix меняет только цвет текста отключённой кнопки;
login-light-ru.png снят с финальной установленной сборки.

Вывод: сначала смотреть реальный frontend, затем переносить его в нативные
компоненты; проверять программную клавиатуру и системные password prompts,
которые не видны в статичном скриншоте. Следующий шаг по продукту — доказать
успешное восстановление, сохранив общий канонический runtime.

Ответ владельцу об архитектуре: сейчас SwiftUI → bridge → встроенный WKWebView
с TypeScript runtime и headless React-проекцией. Rust FFI в iOS не подключён.
Совпадение общего TS source не доказывает TS↔Rust semantic parity. Миграцию
телефона на Rust отложить до измеренного CPU/RAM/energy bottleneck: текущие
подтверждённые дефекты восстановления не устраняются сменой языка.

## 2026-09-13, 13:43 UTC — отмена поиска восстановления

Первое воспроизводимое падение: AbortController нативного BrainVault прерывал
KDF, но не передавался в discoverTowerRestore и HTTP discovery/download.
**3/3 новых регрессии сначала упали**: отменённые запросы возвращали обычные
результаты. Сигнал проведён через общий discovery до обоих fetch. До начала
источника, при обработке ошибки и перед возвратом кандидатов проверяется отмена;
она больше не превращается в ExpectedEmpty/Contradiction. Причина abort сохранена,
native boundary переводит её в прежний BRAINVAULT_ABORTED. SwiftUI после явного
Cancel возвращается к форме без красной ошибки. Сетевой транспорт xln:// уже
отменяет URLSessionTask при WebKit stop; отдельный транспорт не добавлялся.

Проверки: **11/11 Bun tests, 29 assertions**, **2/2 browser E2E** (6.5s),
UI TypeScript, bundled runtime, финальная Debug-сборка и frozen-core:check прошли.
Ни fetch, ни runtime не подменялись: регрессии используют настоящий fetch и локальный
endpoint tower9100; in-flight lookup отменяется после вызова async discovery.
Browser E2E отменяет native entry на переходе KDF → discovery, проверяет
BRAINVAULT_ABORTED, неизменный список IndexedDB и отсутствие запущенного runtime.

**2/2 сценария Simulator (100%)**: отмена реального KDF с 10,000 тестовых shards
возвращает доступную форму с очищенным паролем; последующая попытка с 6 shards
доходит до настоящего tower9100 и честно сообщает об отсутствии копии. Подробный
сетевой abort доказан отдельной регрессией и browser E2E, не выдаётся за ручной
клик по зависшему HTTP в Simulator. Устройство — iPhone 17 Pro / iOS 26.5.
Новая сборка установлена, по окончании теста приложение остановлено.

Артефакты: output/ios-cancel-recovery-2026-09-13/cancelled-ru.png,
retry-result-ru.png и cancel-flow.mp4. Общий `bun run check` вновь остановился на
TEST_WORKSPACE_BUDGET_EXCEEDED: workspace 88,034,304,000 bytes,
max 53,687,091,200. Квоты, пользовательские данные и frozen core не изменены.

Граница результата: отмена поиска/скачивания и KDF; прерывание уже начатого
bootHostedVault или peer.fetchBundles в полёте этим шагом не реализовано.
Положительный restore большого wallet остаётся заблокирован размером копии.
Вывод: проверять отмену на каждой async-границе, а не только исчезновение спиннера.
Следующий шаг — проверить и явно определить поведение Cancel после начала
импорта/подготовки runtime, чтобы интерфейс не обещал отмену необратимой стадии.

## 2026-09-13, 14:43–15:43 UTC — граница отмены перед импортом

Исправлена гонка: ранее поздний Cancel мог установить abort после начала
bootHostedVault и заменить настоящую ошибку запуска сообщением BRAINVAULT_ABORTED.
Теперь native BrainVault отзывает возможность отмены перед boot и публикует
brainvaultFinalizing. Команда cancel возвращает фактический boolean; SwiftUI
убирает Cancel и показывает локализованное завершение подготовки кошелька.
До этой границы KDF и discovery по-прежнему отменяются. Rollback начатого импорта
не обещается; финансовое ядро и его правила хранения этим изменением не менялись.

На существующем локальном тестовом BrainVault проверен настоящий переход в boot:
finalizing=true, lateCancel=false, idleCancel=false. Сохранилась реальная ошибка
WS_HELLO_AUDIENCE_MISMATCH: expected ws://127.0.0.1:8082/relay,
received ws://localhost:5183/relay. runtimeOpened=false: это доказательство
границы отмены и сохранения ошибки, не успешное восстановление. Данные и runtime
не подменялись. Артефакт: output/ios-finalizing-2026-09-13/browser-boundary.json;
рядом reproduce-boundary.ts для существующего локального тестового профиля.

В 15:43 заново собраны bundled runtime (2.18s) и Debug iOS (2.15s), приложение
установлено на iPhone 17 Pro / iOS 26.5. Browser E2E под stand lock: **2/2 PASS
(6.6s)** — отсутствие копии не создаёт пустой runtime; отмена discovery не открывает
кошелёк и не создаёт базы, после завершения cancel возвращает false.
Ручная проверка финальной установленной сборки **не выполнена**: CUA сообщил
«The Mac is locked and automatic unlock could not unlock it». Обход блокировки
не предпринимался; нового скриншота или видео нет. iPhone 18 Pro не проверен.

Этап проверки **4/6 (67%)**: host build, iOS build, установка и browser E2E прошли;
Simulator UI заблокирован, общий check не зелёный. Для неизменного исправления
сохранён уже выполненный bun run check: TEST_WORKSPACE_BUDGET_EXCEEDED,
workspace=87845023744, budgeted=67682485389, max=53687091200 bytes,
reason=contract-invariants. Зависимые проверки прерваны; оставшиеся PASS в логе
не превращают запуск в успешный. Дорогой gate повторно не запускался, квоты и
пользовательские данные не менялись. git diff --check прошёл.

Вывод по методу: контролировать отмену в точке, где начинается запись, а не только
видимость кнопки; для гонки пригоден реальный callback границы без подмены runtime.
При недоступном UI завершать сборку и независимые проверки, явно оставляя ручной
сценарий незакрытым. Следующий шаг: после разблокировки Mac проверить Cancel в
установленной сборке; для положительного boot выяснить источник relay audience
в локальном стенде. Не повторять загрузку 26 MB recovery при неизменном лимите.

## 2026-09-13, 16:43 UTC — relay mismatch локализован в тестовом контексте

Уточнение предыдущего отчёта: mismatch при вызове native BrainVault из страницы
http://localhost:5183 нельзя считать доказанным дефектом iOS. В этом тесте
detectStack получает явно заданный http://127.0.0.1:8082, но browser WebSocket
посылает Origin http://localhost:5183. Канонический resolveConfiguredRelayAudience
выбирает разрешённый оператором audience из Origin; клиент ожидает другой URL.
В приложении NativeSockets использует URLSession без browser Origin. Проверку
подписи/audience ослаблять или менять настройки relay для обхода ошибки не нужно.

Добавлены scripts/native/tests/relay-challenge.swift и relay-audience.ts:
реальный Swift URLSession получает начальный challenge, канонический TS decoder
проверяет сообщение; два настоящих browser-сокета сравнивают proxy и смешанный
Origin. Ни серверы, ни WebSocket, ни runtime не подменяются. Нет кошельков, ключей,
signed hello или финансовых команд. **3/3 PASS** сначала на macOS (2.45s), затем
с Swift executable внутри booted iPhone 17 Pro / iOS 26.5 (1.97s), оба запуска
под stand lock. Mach-O LC_BUILD_VERSION подтверждён: IOSSIMULATOR, SDK/minOS 26.5.
Swift driver вывел предупреждение о sysroot; platform metadata и успешный запуск
в Simulator проверены отдельно, предупреждение не скрыто.

Факты: URLSession direct ожидает и получает ws://127.0.0.1:8082/relay;
браузер через свой proxy ожидает и получает ws://localhost:5183/relay;
браузер напрямую к 8082 с Origin 5183 воспроизводит прежнее несовпадение.
Артефакт: output/ios-relay-2026-09-13/simulator-transport.json. Это только первый
challenge, не доказательство полной аутентификации, boot или восстановления.
Прежний reproduce-boundary.ts сохраняется как историческое воспроизведение
границы отмены; не использовать его смешанный транспорт для оценки native boot.

Воспроизведение: скомпилировать relay-challenge.swift через xcrun swiftc
-parse-as-library -target arm64-apple-ios26.5-simulator -sdk <iphonesimulator SDK>
-o /tmp/xln-ios-relay-challenge-simulator; после stand:status выполнить
XLN_TEST_SIMULATOR_UDID=EEECD8AA-0232-4027-998D-3C65B3754C5B
bun run stand:run --reason simulator-relay-origin-proof --timeout-ms 60000 --
bun scripts/native/tests/relay-audience.ts /tmp/xln-ios-relay-challenge-simulator
output/ios-relay-2026-09-13/simulator-transport.json.

Debug-сборка приложения прошла (1.81s), runtime и UI приложения не изменялись.
Проверки этапа **4/6 (67%)**: три транспортных случая и сборка прошли; ручной UI
снова недоступен из-за блокировки Mac, общий check остаётся незелёным из-за ранее
подтверждённого workspace budget. При неизменном внешнем барьере дорогой check
не повторялся. git diff --check прошёл. Скриншотов, видео и проверки iPhone 18 Pro
в этом запуске нет. Блокировка Mac не обходилась: simctl запустил тестовый
процесс в уже работающем Simulator, без управления экраном macOS.

Вывод по методу: воспроизводить нативный транспорт в Simulator, прежде чем
переносить ошибку Chromium на приложение. Улучшена проверка, функциональный код
не менялся. Следующий шаг — ручная проверка Cancel после разблокировки Mac;
положительное восстановление по-прежнему требует устранения причины большой
recovery-копии, а не исправления этого browser-only relay mismatch.

## 2026-09-13, 17:44 UTC — истёкшая котировка в нативном подтверждении

Конкретный дефект: native commands выдаёт авторизацию на 60 секунд, но SwiftUI
не получал deadline и показывал Confirm после его истечения. Runtime корректно
отклонял такую попытку; интерфейс заставлял пользователя сначала получить ошибку.
Теперь payment/swap/order quote передают expiresAt из того же ticket.expires;
для ответа needsCapacity без авторизации передаётся 0. TTL, одноразовое потребление
ticket и финансовые расчёты не менялись.

WalletTicket и WalletRouteOption вынесены из WalletActionSheet в WalletQuote.swift
(Foundation DTO, добавлен в Xcode target). expiresAt обязателен: старый bundle без
поля вызывает decode error вместо бессрочного подтверждения. Swift сравнивает
deadline в миллисекундах с текущим временем; точная граница соответствует прежней
runtime-проверке expires < Date.now(). После срока TimelineView показывает
локализованное сообщение и Refresh quote / «Обновить котировку». Обновление
проходит обычный quote-путь, сохраняя выбранный маршрут. Новые условия требуют
отдельного Confirm; автоматической отправки нет.

Проверки **6/8 (75%)**:
- Swift decoder/deadline vectors внутри iPhone 17 Pro / iOS 26.5: **5/5 assertions**,
  до срока, на точной границе, после срока, точная строка минимальной доли WETH,
  обязательный deadline. Это входные векторы DTO, не подмена кошелька/сервера.
- UI TypeScript (6.72s), native host build (2.13s), Debug app build (4.93s),
  установка и browser module smoke (1.65s) прошли. Браузер загрузил изменённый
  native financialCommand без page errors; платёж/своп smoke не выполнял.
- Ручной UI expiry/refresh не проверен: CUA вновь сообщил о заблокированном Mac.
  Общий check остаётся незелёным; при неизменном внешнем workspace budget барьере
  дорогой gate не повторялся. git diff --check прошёл.

Артефакты: output/ios-quote-expiry-2026-09-13/simulator.log, app-build.log,
browser-smoke.json. Нового скриншота/видео нет. Simulator executable собран с
SDK/target IOSSIMULATOR 26.5 и запущен через simctl; это проверка Swift DTO и срока,
не сквозной финансовый E2E и не проверка iPhone 18 Pro. Проект не объявляется
готовым к production. Сетевые серверы, ключи и средства не менялись.

Вывод по методу: передавать срок существующей авторизации в UI, не дублировать
TTL и не ждать отказа на Confirm. Проверку времени можно выполнять нативно без
управления заблокированным экраном. Следующий шаг после разблокировки Mac:
получить настоящую котировку, дождаться истечения, обновить и проверить новые
условия перед отдельным подтверждением. Затем вернуться к положительному recovery.

## 2026-09-13, 18:44 UTC — платежный баннер после блокировки

Воспроизведён дефект в настоящем WalletModel: receipt принимался при заблокированной
модели, lock не очищал notice, а неудачная попытка открытия сохраняла баннер прошлого
сеанса. Новый scripts/native/tests/receipt-session.swift сначала дал **0/5 PASS**
в iPhone 17 Pro / iOS 26.5. Тест компилирует реальные Wallet*.swift; передаёт входные
UI-сообщения и вызывает настоящие lock/openBrainvault. WebView и сервер не подменены;
failed-open заканчивается штатным отсутствием host до деривации или обращения к сети.
Тест не читает entropy и не создаёт кошелёк. Проверка наличия Keychain выполняется
обычным init модели, без изменения элементов Keychain.

Минимальная правка WalletModel: notice очищается при lock и в начале unlock /
openBrainvault; receipt принимается только для unlocked, не lockRequested модели
с видимым snapshot. Даже отложенный из-за busy lock очищает баннер сразу. Журнал
подтверждённых операций не меняется; подавляется только уведомление скрытого кошелька.
Расчёты, TS-runtime, мостовой формат и отправка команд этим шагом не менялись.

После правки те же **5/5 assertions PASS (0.35s)**: сообщение при исходной блокировке,
очистка при lock, позднее сообщение после lock, очистка при deferred lock, очистка
при failed-open. Debug app build прошёл (3.21s), новая сборка установлена.
Проверки этапа **3/5 (60%)**: Simulator model regression, сборка, установка прошли;
ручной UI и общий check не закрыты. CUA в этот раз вернул timeoutReached (-10005);
в предыдущих запусках Mac был заблокирован. По тайм-ауту текущий статус блокировки
не утверждается. Изменений внешнего workspace budget барьера не подтверждено;
дорогой общий check повторно не запускался и зелёным не объявляется.
git diff --check прошёл. Компилятор теста показывает существующие предупреждения
о deprecated Text + Text в других Wallet view; они не скрыты и в эту правку не входят.

Артефакты: output/ios-receipt-session-2026-09-13/before.log, after.log,
app-build.log. Это проверка состояния реальной Swift-модели, не живой платёж,
визуальный E2E или тест iPhone 18 Pro. Нового видео/скриншота нет. Доставка нового
платежного баннера в успешно открытой сессии этим регрессионным тестом не проверена.

Вывод по методу: сначала получить падение на реальной модели, затем менять только
первую нарушенную границу. Так получена проверяемая функциональная правка без
подмены runtime и без зависимости от заблокированного экрана. Следующий шаг при
возвращении доступа к UI: новый подтверждённый тестовый платёж → lock → reopen;
проверить и появление актуального баннера, и отсутствие старого. Отложенные expiry /
refresh и положительное восстановление по-прежнему не считаются завершёнными.

## 2026-09-13, 19:45 UTC — закрытие отложенных UI-проверок заблокировано

Новых функциональных изменений нет. Вместо накопления следующих правок выбраны
два незакрытых сценария: expiry → refresh настоящей котировки и актуальный payment
notice → lock → reopen. **0/2 ручных сценариев (0%)** выполнено: CUA сообщил
«The Mac is locked and automatic unlock could not unlock it». iPhone 17 Pro /
iOS 26.5 остаётся booted, stand свободен, но это не даёт доступ к ручному UI.
Доступ появится после ручной разблокировки Mac владельцем; обход не предпринимался.

Независимо обновлено доказательство второго барьера. Read-only du -sk (3.89s),
тот же способ измерения всего workspace, что в assertWorkspaceBudget, вернул
88,649,856 KiB = **90,777,452,544 bytes (84.54 GiB)**. Текущее окружение не задаёт
XLN_TEST_WORKSPACE_MAX_BYTES; канонический предел **53,687,091,200 bytes (50 GiB)**.
Workspace всё ещё выше лимита. Это свежая диагностика размера, не запуск общего
check и не его положительный результат. Данные и квоты не менялись.

Поскольку код приложения не менялся, прошлые сборки/регрессии и дорогой общий
check повторно не запускались. Нового скриншота, видео или завершённого E2E нет.
git diff --check прошёл. Предыдущие component PASS не повышены до end-to-end PASS.

Вывод по рабочему циклу: при повторном внешнем блокере не добавлять очередную
функцию ради часового отчёта. Сначала закрыть уже накопленные проверки; проверять
дешёвым read-only способом, действительно ли изменился внешний барьер. Следующий
шаг — после разблокировки Mac выполнить expiry/refresh на установленной сборке,
затем payment notice/lock/reopen. Положительное recovery остаётся отдельным
незавершённым обязательством.

Последняя проверка 2026-09-14 16:03 UTC: Mac по-прежнему заблокирован (прямой ответ CUA),
iPhone 17 Pro / iOS 26.5 booted. Те же ручные сценарии остаются **0/2 (0%)**.
Новых изменений, сборок, тестовых переводов или медиа нет; прежние проверки
и измерение workspace не повторялись. Следующий шаг не изменён: разблокировка
Mac, затем expiry/refresh на установленном приложении.
