# React batch preflight read contract

Status: Runtime read authorization requested; no Runtime/API implementation in this increment.

- Current SHA: `cc11c96158c9f413869424d9c74d022cbe0311ec` plus the preserved working changes.
- Last green command: `bun test tests/frontend/payments tests/frontend/assets/entity-input-projection.test.ts` with Bun 1.4.0: 60 tests, 305 assertions.
- First red boundary: `core/api/runtime-adapter/resolve.ts` compacts outgoing debt to 20 token buckets / 20 debts per bucket, reserves to 100 entries and each batch operation list to its last 50 entries. Nested settlement diffs and collateral pairs are also truncated. Both `view-frame` and `entity/<id>` reads use compaction. There is no completeness marker; an issue-free simulation on these projections does not prove an issue-free complete draft. React currently supplies a null warning, and its review key binds the compact batch rather than all operations.
- Artifact: `frontend/bridges/wallet/payments/pending-batch-preview.ts` now owns the unchanged retained preview/simulation adaptation. `tests/frontend/payments/frontend-wallet-batch-preflight.test.ts` covers debt priority, sufficient reserves, repaid/unrepaid deficits, settlement perspective, presentation order and input immutability using the real canonical simulation.
- Next single command after API authorization: `sed -n '1920,2050p' core/api/runtime-adapter/resolve.ts`. Implement an Entity-scoped, read-only batch-preflight result from one complete committed snapshot using `simulateDraftBatchReserveAvailability` and the canonical outgoing-debt totals helper. Bind the result to Runtime identity/height, Entity identity, exact draft identity and sent-batch identity. Return bounded issue/summary data and truthful operation counts without exposing redacted proofs, signatures or secrets. Re-read before enqueue, reject Entity/draft changes, and preserve all existing Runtime command admission. A new compare-and-submit transaction would require separate protocol scope; an advisory read alone cannot make the subsequent command atomic.
- Remaining final gates: regressions where the decisive debt is the 21st, reserve token the 101st, spend the 51st operation or nested pair/diff beyond its projection bound; complete-state results must differ from the misleading compact results. Cover debt, deficit repayment, withdrawals/settlements, Entity/draft changes, remote and embedded transports, malformed responses and unresolved reads. React must display the issue, disable unsafe broadcast and recheck before submission. Prove no submission on rejection and finality for the valid batch in three viewports, then update plan/evidence and run root checks. No financial formulas, persistence schemas, production cutover or deployment are authorized by this frontend continuation.
