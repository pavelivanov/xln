# React frontend migration historical evidence

Archived from `plans/react-frontend-migration.md` on 2026-09-15. These paragraphs preserve prior commands, failures, source identities and implementation receipts. Their counts and readiness statements describe their individual revisions; they are not current acceptance status. The [execution plan](../../plans/react-frontend-migration.md) owns current status and remaining requirements.

### Initial assessment baseline (superseded by current evidence below)

Commands below were run on the source plus working diff above. These results establish a baseline, not complete migration acceptance.

| Check                                                                                  | Result                                                                                                                                                                                                |
| -------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `bun test tests/frontend`                                                              | **1,510 pass / 0 fail**, 11,617 assertions, 245 files, 7.48 s. Run with localhost access for the existing isolated-server tests. Log: `/private/tmp/react-plan-20260914-units.log`.                   |
| Seven ownership/route/capability/platform/parity/Wallet-flow/check-scope test files    | **42 pass / 0 fail**, 1,093 assertions, 1.121 s. Exact command is in section 7.                                                                                                                       |
| Frontend `bun scripts/check.ts --all --level=local`                                    | **All four apps and tooling pass; 874 scanned files / 0 unsafe-type findings.** Log: `/private/tmp/react-plan-20260914-local.log`. No fresh production build in this assessment.                      |
| Frontend `bunx playwright test --config playwright.react.config.ts --list`             | **423 registered cases / 41 files**, across 390×844, 1366×900, 1920×1080. Includes nine cases from the new BrainVault spec. Registration only.                                                        |
| Frontend `bun scripts/checks/parity-audit.ts`                                          | 20 routes: 18 complete / 2 partial; 12 capabilities: 7 implemented / 5 in progress; 3 declared gaps. These are manually maintained classifications, not completion percentages. Its B9 text is stale. |
| `bun tools/release-snapshot/migrate-hanko-envelopes.ts . --check`                      | **0 pending files / verified=true.** B9 is resolved; no decoder/trust-policy change is needed.                                                                                                        |
| Explicit historical release verifier                                                   | **473 files verified**, release `sha256-c5f7100e8f48273d4b5670243df18692c9c9316283387da30d51fbe961492b31`. Integrity only; it predates current BrainVault changes.                                    |
| `git apply --check --unidiff-zero docs/frontend/react-frontend-cutover-commands.patch` | Exit 0. Patch applicability does not establish working canonical consumers.                                                                                                                           |
| `bun run check`, bounded to 30 s                                                       | **Exit 127**, `cargo: command not found` in `rscore:fmt`; other aggregate children are cancelled. No frozen-core bypass. Log: `/private/tmp/react-plan-20260914-root.log`.                            |

Current Bun is 1.3.14; CI declares 1.4.0. Record runner versions with final evidence; do not silently update dependencies during this planning work. `adb` remains absent from current PATH. Rust/Cargo 1.94.1 are installed: process-local `PATH="/opt/homebrew/opt/rustup/bin:$PATH"` makes the repository-pinned toolchain available. The current root check exits 1 in 25.54 s at `FORBIDDEN_DEPENDENCY api/public->api/server core/api/public/runtime-public.ts:31:../server/control/control-board-governance`; the file is unchanged in this migration. Sibling checks are cancelled, so Rust compilation remains unverified. The earlier exit-127 logs remain historical evidence; see `output/playwright/react-retirement-dependencies-20260915/root-check.log` for the current failure. Earlier root/package version drift (`package.json` 0.1.32 versus `VERSION` 0.1.31) was not repaired; recheck release metadata with its owner before packaging.

Retained evidence, not rerun here: `output/react-finish-20260910/` (Docs, ownership, history, B9, artifact/PWA/rollback/native), `output/react-finish-20260911/ops-matrix/` (21 completed cases), `output/react-finish-20260911/ops-next/` (35 completed final cases), `output/plan-wallet-20260908/`, `output/plan-ops-20260908/`, `output/framework-isolation-20260909/`. Some runs overlap or use older bytes: do not sum them into a current pass rate. Prior final autoplay regression covered mobile/laptop only; wide remained incomplete. The prior shared Graph/history run timed out at Fit network without establishing a rendering defect.

### Corrections to the previous plan

- I09, V04a, V06a–V08a and B9 are complete implementation work. Their old step-by-step implementation sections are retired, not reopened.
- **R03 implementation is committed at `cc11c96158c9f413869424d9c74d022cbe0311ec`**, with late-completion regressions added during this continuation. It uses remote default plus an explicit local-create/recover entry. Acceptance and decision provenance are not established by this assessment; preserve the work and do not infer approval merely from code or re-ask an already answered choice.
- **B1 is a policy conflict, not a demonstrated admission bug.** `core/account/tx/admission-policy.ts:20` explicitly excludes six lending variants under D3/FX-2. `proofs/fixes.md:37` records the decision. The browser requirement at `frontend/tests/react-candidate/wallet/account/wallet-account-workspace.spec.ts:311` demands a positive pool/loan/repayment flow. An owner must reconcile these requirements.
- V03 reconciled the stale B9, R03 and B1 descriptions in `parity-audit.ts`, `capabilities.ts` and `wallet-flow-audit.ts`; B9 again verifies with zero pending migrations. Other financial/platform/cutover gates remain open.
- The cutover review binds an older 470-file release. The deletion list is now refreshed to 277 paths with a tested, unapplied guard patch; remaining adapter/source-reading tests still require migration. Do not apply retirement mechanically.

## Consumer and shared-owner implementation receipts

Consumer implementation is underway under the owner’s request to implement the recommended order. Preview, neutral release ownership and explicit npm/server serving are implemented; see `docs/frontend/react-frontend-cutover-review.md` for exact commands and `output/playwright/react-consumers-20260914/` for evidence. The historical release remains `sha256-c6a559bf37c43a6e26bc3b0b99e9af40180faec821b674d70912222592abf517` (470 files). The local npm archive contains 485 files and was installed offline into a disposable directory; this does not certify the final application release or production deployment. Verification: **1,513 frontend tests / 11,644 assertions**, all four local checks, Runtime TS7 and unchanged frozen-core root pass. Dev/native/deployment/CI wiring, full packaged launcher acceptance and final candidate checks remain open. The requested extension of non-browser verification budgets is pending; retain 30 s until the owner answers. The dev continuation is prepared under `XLN_DEV_FRONTEND=react`; its temporary selector and Svelte roles must be removed at C02. `output/playwright/react-dev-consumer-20260914/` retains **1,542 passing tests / 11,814 assertions** (frontend plus affected dev-supervisor suite), four-app/tooling typecheck (24.50 s), actual Node HTTPS/upgrade shutdown proof, the failed root launch and executed runner. The retained `ui/` dependencies were installed from its unchanged frozen lockfile. The earlier dev test failure was missing those dependencies; it now passes. Root dev's default TS preflight does not require Cargo: the Rust freshness gate applies only when `XLN_HLT_ENGINE=rust`. Cargo remains required for the final repository gate. The final root gate exits **127** in **19.18 s**, missing Cargo; Runtime TS7 and frozen-core checks pass separately. Logs are retained in the same dev consumer evidence directory. The cutover command patch is now an explicit zero-context diff; verify it with `git apply --check --unidiff-zero docs/frontend/react-frontend-cutover-commands.patch`.

C01b deliverable: complete consumer diff, new exact CLIs and their tests, selected release and generated-input provenance, regenerated retirement list, byte-verification/package/preview evidence, and rollback identity with provenance. The review's old 470-file candidate and local prior candidate are not a verified production rollback target. Request C02 authorization only after this concrete package is reviewable; unresolved acceptance remains plainly open.

Native/CI consumer evidence is in `output/playwright/react-native-consumer-20260914/`: **1,548 frontend/native tests / 11,842 assertions / 254 files** pass (9.11 s), and the expanded four-app/tooling check passes (23.91 s). Strict native checking exposed the obsolete `bundledWebRuntime: false` option; it is removed according to the Capacitor migration documentation, and native tooling is now included in the regular frontend tooling typecheck. The early contract run accidentally discovered a loose evidence source snapshot as a test; the snapshot was byte-verified into an archive, and the actual tests pass. The exploratory extension ZIP has exact verified payload bytes but a 0.1.32 filename with a 0.1.31 manifest; it is **not a final release artifact**. The new version check prevents that mismatch, with no metadata changed without the pending owner decision. Desktop executable/device acceptance remains unfulfilled. The required final root check exits **127** in **19.85 s** because Cargo is missing; Runtime TS7 and frozen-core verification pass separately. The unapplied C02 command patch now also retargets frontend native build/sync/open aliases to the explicit verified-release consumer. No hosted workflow, signing, publication or deployment was run.

Deployment consumer evidence is in `output/playwright/react-deployment-consumer-20260914/`. The exact historical pair is install `sha256-c5f7100e8f48273d4b5670243df18692c9c9316283387da30d51fbe961492b31` (473 files) and update `sha256-c6a559bf37c43a6e26bc3b0b99e9af40180faec821b674d70912222592abf517` (470 files). The actual canonical deployment entry point passes eight operations through disposable nginx in **12.15 s**, including six-route hash checks and automatic rollback after a real edge-header misconfiguration. Final state restores the install ID with the update as rollback; both source trees reverify. This proves HTTP consumer integration, not Runtime/auth/WS, production TLS, or final application bytes. The deployment store now has one neutral owner at `packages/frontend-release/deployment.ts`; HTTP reads share that owner. New tooling is included in the four-app local typecheck, which passes in **25.05 s**, with **876 files / zero unsafe-type findings**. Frontend units pass **1,521 / 0 failures**, **11,729 assertions / 248 files**, 8.19 s. The seven deployment units pass and the focused production frontend wiring contract passes. Six unrelated production-startup assertions remain red in the broader two-file suite; retain their log instead of weakening them. The prepared nginx template still needs comparison with the actual deployed configuration and an authorized installation. The first root check caught the adapter taking the orchestrator to 3,001 lines; the equivalent two-line conditional restores the existing limit. The final root check passes the size/artifact gates, then exits **127** in **20.43 s** at missing Cargo; sibling checks are cancelled. Runtime TS7 passes independently in **1.40 s**, frozen core remains unchanged, and `git diff --check` plus unapplied C02 patch applicability pass. Initial production store setup requires an explicitly identified verified rollback baseline; the offline `initialize` command is not permission to invent one.

Installed npm continuation evidence is in `output/playwright/react-npm-launcher-20260914/`. The startup preflight still required the retired `app/app.html`, despite the explicit release layout; that mismatch is fixed by bundling the neutral verifier and checking the selected release before state creation or daemon contact. `bun scripts/release/check-xlnfinance-frontend.ts --archive <absolute-tgz> --output <receipt-json>` offline-installs the actual archive, copies it before mutation to avoid altering hard-linked package caches, and exercises the real CLI for null/path-escape/extra-field pointers, corrupted Wallet entry and missing manifest. Valid/restored preflight and unchanged installed bytes are verified. **8 cases pass / 470 release files / no state created**, 6.27 s; pack produces **486 files** in 4.63 s. Prepared CI runs this check after launcher packaging and before signing; four workflow contracts pass. The separately installed server passes six release hashes plus real pairing/authenticated WebSocket checks in **9.06 s**. Its ordinary SIGTERM exit is 143 and its open WebSocket closes with 1006; the original harness's incorrect exit-0 expectation and failed log are retained. This does not certify graceful persistence shutdown, restart recovery, or the full launcher lifecycle. Nginx PIDs 1569/1589 still own 8080; the pending temporary stop/restore decision is unchanged. No unrelated listener was stopped, no protocol behavior changed, and no package was published. Version metadata remains the pending 0.1.31/0.1.32 owner decision. Final frontend units pass **1,522 / 0 failures**, **11,735 assertions / 248 files** (8.57 s), and all four apps/tooling pass in **24.96 s**. The root gate exits **127** in **19.45 s**, missing Cargo; sibling checks are cancelled. Frozen core is unchanged, `git diff --check` passes, and the unapplied cutover patch remains applicable.

Retirement preparation evidence is in `output/playwright/react-retirement-20260915/`. The refreshed list has **278 existing paths**: **272** retained-source files, four Svelte configs and two obsolete preview/build wrappers. No source retirement is applied. Six forwarding imports in six frontend-owned test files now reference their canonical owners; the focused **26 tests / 1,171 assertions** pass. The previously stale BrainVault test's telemetry imports, input panel and worker-cleanup checks now target the current browser/React owners without changing their behavior. The active graph guard follows neutral release modules as well as frontend modules. The new **unapplied** `docs/frontend/react-frontend-retirement-guards.patch` updates seven inventory/test files for C02. In a disposable copy without the 278 paths, **33 tests / 1,041 assertions** pass across six focused guard suites. Full retirement remains open: **15 direct import edges** use legacy adapters or different interfaces after the Solvency and recovery coverage migrations, and additional source-reading assertions still depend on Svelte. Their exact consumers are recorded in the cutover review and import report. These targeted checks do not prove all tests survive retirement, authorize C02, or close remaining product/platform acceptance. Current frontend units pass **1,522 / 0 failures**, **11,735 assertions / 248 files** (8.15 s), and four-app/tooling checks pass in **24.68 s**. The final root gate exits **127** in **21.98 s** at missing Cargo; sibling checks are cancelled. Frozen core remains unchanged, both unapplied patches pass applicability checks, and `git diff --check` passes.

Recovery coverage parity was restored during retirement preparation. `frontend/bridges/wallet/recovery-coverage.ts` is the single projection owner for retained Svelte and React; its calculation/classification body is byte-identical to the prior helper. React now displays local state, configured versus observed tower/last-resort coverage, peer discovery outcomes and per-service receipt/failure details from the canonical recovery-services view. Existing recovery writes and protocol data are unchanged. Coverage now observes canonical vault metadata and Runtime-scoped discovery changes. Same-Runtime observations update evidence and authority without replacing the validated service draft; stale draft/Runtime callbacks are rejected and subscriptions are removed on teardown. Cross-tab discovery uses native storage notifications, and same-tab writes/clears notify observers without changing the storage schema. The 11 existing recovery tests preserve failure precedence, URL deduplication, disabled services, stored-byte labels and typed peer failures. The focused boundary run passes **17 tests / 247 assertions**. Current retirement preparation has **277 paths / 271 retained-source files / 15 direct import edges**; no Svelte component was retired. The guard patch is regenerated for the relocated helper and new view inventory; the six focused guard suites pass in a disposable copy with all 277 retirement paths absent.

The real restore/enrollment/device-wake flow passes **3/3 viewports** in **25.59 s** on the final recovery display source. Browser-error and page-containment checks remain intact. Screenshot review caught inherited onboarding styles clipping peer detail; scoped styles and a per-detail overflow assertion correct that defect. All three final screenshots were inspected. Four-app/tooling typechecks pass in **25.29 s**. Commands, the initial command-form error, before-layout screenshots, final screenshots and logs are in `output/playwright/react-recovery-coverage-20260915/`. Earlier 475-file release acceptance below predates this visible change and must not be treated as acceptance of these new bytes. The new build/assembly passes in **11.59 s** and creates **477-file** release `sha256-40cace0df681a5d03f3def4f64eb72b2e366acbc3e0e204c987082c2c8f5fbf4`. Its added immutable recovery scenario passes **3/3** viewports in **24.19 s**, with no development-source network fetches and unchanged before/after release verification. The same scenario helper now serves development and artifact tests, preserving its existing recovery, custody-storage, enrollment and device-wake assertions. `provenance.json`, `source-snapshot.tar.gz`, `source.patch` and `generated-manifests.tar.gz` bind the 119-file working source snapshot, relocated projection-body hash, seven generated-input manifests and release manifest. Test-harness extraction followed the build; application source is unchanged. Frontend regressions pass **1,522 tests / 11,739 assertions** in 8.08 s. The root gate exits **1** in **24.19 s** at the same unchanged `api/public -> api/server` Runtime dependency violation; sibling gates are cancelled. No Runtime fix or root-gate relaxation is included. The existing artifact matrix initially passes **8/9**: wide Graph remains visibly running when the default 5 s assertion expires. Its trace/screenshot are preserved. The scenario-completion assertion now permits 15 s within the unchanged outer process budget, retaining exact 126-step/history/Solvency/Live assertions; the focused wide rerun passes in **14.56 s**. Together with the three recovery cases, **12 distinct artifact cases have passing receipts** on this release across bounded runs. This is not a single all-green matrix invocation. Subsequent PWA and isolated activation/rollback checks pass on the explicit 475-file install / 477-file update pair (18.27 s combined), and native staging/copy checks pass in 1.62 s with 95 Wallet payload files, 276 Capacitor shell files and 199 packaged shell files. Both lifecycle inputs and the copied 477-file source reverify. These results precede the live-observer application change and are not acceptance of later bytes.

Live recovery continuation evidence is in `output/playwright/react-recovery-refresh-20260915/`. The production metadata observer passes focused **20 tests / 265 assertions**, including receipt arrival, typed discovery changes, Runtime switching, unsaved-draft preservation, stale observer rejection and idempotent cleanup. The real tower-upload browser flow confirms that a background receipt updates the mounted panel while an unsaved service remains visible and absent from persisted configuration: **3/3 viewports pass** (laptop 13.06 s, mobile/wide 22.53 s). No synthetic tower response or application-source import supplies the receipt. Observation failures remain visible and disable writes; recovery mutation errors stay distinct from observation errors. All four apps and tooling pass in 23.92 s. The initial 477-file lifecycle and native-copy successes above remain bound to those historical bytes; new acceptance must use the observer build.

Move allowance retirement preparation now uses `frontend/packages/ui/src/entity/move/move-allowance.ts` as the shared owner for React and retained Svelte. The helper body is unchanged from HEAD; only its import paths changed. All five existing allowance tests preserve their assertions, and shared Move validation uses the same allowance comparison. React displays both current and required allowance. The real transfer → insufficient approval → sufficient approval → deposit-draft flow passes **3/3 viewports / 23.05 s** including startup/cleanup; all three screenshots were inspected. The test sets the insufficient allowance through a real 0.5 USDC approval before approving the required 1 USDC, because the fixture chain persists between viewports. Its initial incorrect zero-allowance assumption and failed wide trace remain preserved. No synthetic chain response supplies positive evidence.

At the Move increment, preparation had **276 retirement paths / 270 retained-source files / 14 direct legacy imports**; the subsequent batch extraction below reduces the import count to 13. The six focused guard suites pass without the 276 paths in a disposable copy (**33 tests / 1,042 assertions / 1.48 s**); no Svelte retirement was applied. All four apps and tooling pass in **23.67 s**; the full frontend suite passes **1,525 tests / 11,754 assertions / 8.37 s**. The required root check exits **1 / 26.06 s** at the unchanged `api/public -> api/server` dependency in `core/api/public/runtime-public.ts:31`; its sibling gates are cancelled. The independently executed retained Svelte check passes with **0 errors / 0 warnings / 11.87 s**, verifying its retargeted import; its initial command-help-only invocation is retained and is not counted as a check. Frozen core remains unchanged. Evidence is in `output/playwright/react-move-allowance-20260915/`, including `helper-relocation.json`, `import-report.json`, `focused.log`, `all.log`, `local.log`, and `without-svelte-receipts.json`. This visible application change is newer than the 477-file release below; its previous artifact/native/PWA acceptance is not acceptance of the Move change. Rebuild and bind affected final-release evidence once the remaining implementation is ready.

Pending-batch retirement preparation extracts the existing count, draft/sent selection, eligibility and action-payload helpers into `frontend/packages/runtime-client/src/payments/pending-batch-state.ts`. React and retained Svelte consume that owner directly. Count/state/eligibility bodies are unchanged; the payload builder takes React’s existing `manual-clear-from-ui` reason explicitly and retains `global-batch-bar-clear` as the global-bar default. Existing stale-review, nonempty/in-flight and terminal-failure checks are unchanged. All five preview test cases retain their assertions; the React clear check now verifies the complete payload. No reserve-preflight, settlement, consensus or financial calculation changes are included. Remaining reserve-preview/preflight migration stays open.

Current retirement evidence is **276 paths / 270 retained-source files / 13 direct legacy imports**, in `output/playwright/react-batch-state-20260915/import-report.json`. Focused checks pass **19 tests / 247 assertions**; all four apps/tooling pass in **25.52 s**. Retained Svelte passes **0 errors / 0 warnings**, full frontend units pass **1,525 tests / 11,755 assertions**, and disposable retirement guards pass **33 tests / 1,043 assertions**; the combined sequential workload takes **19.82 s**. The required root gate exits **1 / 23.58 s** at the same unchanged `api/public -> api/server` dependency (`core/api/public/runtime-public.ts:31`); sibling gates are cancelled. Frozen core remains unchanged and both unapplied cutover patches pass applicability checks. The initial missing ownership entry/direct core type import failure is retained in `focused.log`; the final module uses the existing public type boundary and inventory, without weakening guards. This increment is non-visual and preserves command payloads, so it does not require another browser matrix. Final artifact acceptance still must include the preceding visible Move change and these new shared-module bytes.

## Batch action verification before Health restoration

The latest visible Move allowance change and subsequent shared batch changes are newer than the built 477-file release in section 7. Move's real insufficient/sufficient allowance flow passes all three viewports. Batch clearing, collateral broadcast/finality and local settlement failure/rebroadcast/finality pass **9/9 cases across all three viewports**, in separate bounded workloads of **11.40 / 13.44 / 37.33 s**. Clear review screenshots were inspected at all three sizes; the mobile retry screenshot was also inspected. Existing browser assertions preserve unchanged balances after clearing, exact collateral changes, stale Entity review invalidation, and expected 503-only errors during the deliberate outage. Evidence: `output/playwright/react-batch-actions-20260915/{clear,broadcast,retry}.log` and corresponding screenshot directories. All four apps/tooling pass in **25.19 s**; **19 focused tests / 248 assertions** pass. The full frontend suite passes **1,525 tests / 11,749 assertions**; retained Svelte reports **0 errors / 0 warnings**; six guard suites pass **33 tests / 1,044 assertions** in a disposable copy without all 275 retirement paths. These three checks complete in **20.55 s**. Logs: `consumers.log`, `units.log`, `svelte.log`, `without-svelte-tests.log` and `without-svelte-receipts.json` in the same evidence directory. The current root check exits **1 / 25.83 s** at unchanged `FORBIDDEN_DEPENDENCY api/public->api/server core/api/public/runtime-public.ts:31:../server/control/control-board-governance`; aggregate sibling checks are cancelled. `frozen-core:check` independently reports unchanged root `0x4eccf4492e5d085b24162f86d327e003c36b7e2a90ad527db1653fde391946a7`. Both unapplied cutover patches remain applicable and `git diff --check` passes. Root and frozen logs are retained alongside the frontend evidence. No new immutable release is claimed by these development runs.
